/** Visual treatment of a {@link import("./Button.js").Button | Button}. */
export type ButtonVariant = "primary" | "secondary" | "outline" | "ghost" | "accent" | "destructive";

/** Padding / font-size / radius scale of a Button. */
export type ButtonSize = "sm" | "md" | "lg";
