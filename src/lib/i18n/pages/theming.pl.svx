---
title: Motywy
---

<svelte:head>
	<title>Theming - FancyUI Docs</title>
</svelte:head>

# Motywy

FancyUI używa niestandardowych właściwości CSS (przestrzeń kolorów OKLCh) do tworzenia motywów, w pełni zgodnych z konwencjami shadcn-svelte.

## Konfiguracja CSS

Zaimportuj arkusz stylów FancyUI w pliku CSS swojej aplikacji (np. `src/app.css`):

```css
@import "tailwindcss";
@import "fancy-ui-svelte/tailwind.css";
```

## System kolorów

Wszystkie kolory używają przestrzeni kolorów **OKLCh** dla percepcyjnie jednolitych gradientów:

```css
:root {
  --background: oklch(1 0 0);
  --foreground: oklch(0.129 0.042 264.695);
  --primary: oklch(0.208 0.042 265.755);
  --primary-foreground: oklch(0.984 0.003 247.858);
  --muted: oklch(0.968 0.007 247.896);
  --muted-foreground: oklch(0.554 0.046 257.417);
  --accent: oklch(0.968 0.007 247.896);
  --border: oklch(0.929 0.013 255.508);
  --card: oklch(1 0 0);
  --card-foreground: oklch(0.129 0.042 264.695);
}
```

## Tryb ciemny

Tryb ciemny jest aktywowany przez klasę `.dark` na elemencie nadrzędnym. FancyUI nadpisuje wszystkie tokeny:

```css
.dark {
  --background: oklch(0.05 0 0);
  --foreground: oklch(0.984 0.003 247.858);
  --primary: oklch(0.929 0.013 255.508);
  --card: oklch(0.1 0.005 265);
  --border: oklch(1 0 0 / 8%);
}
```

## Tokeny animacji

Kontroluj czas trwania animacji globalnie:

```css
:root {
  --animation-fast: 150ms;
  --animation-normal: 300ms;
  --animation-slow: 500ms;
  --animation-slower: 1000ms;
}
```

## Funkcje wygładzania

```css
:root {
  --ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
  --ease-out: cubic-bezier(0, 0, 0.2, 1);
  --ease-in: cubic-bezier(0.4, 0, 1, 1);
  --ease-bounce: cubic-bezier(0.34, 1.56, 0.64, 1);
}
```

## Gradienty tęczowe

Używane przez `RainbowButton`, `GradientButton` i inne efekty gradientowe:

```css
:root {
  --rainbow-1: hsl(0 100% 63%);
  --rainbow-2: hsl(270 100% 63%);
  --rainbow-3: hsl(210 100% 63%);
  --rainbow-4: hsl(195 100% 63%);
  --rainbow-5: hsl(90 100% 63%);
}
```

## Dostosowywanie

Nadpisz dowolny token we własnym CSS, aby dostosować wygląd:

```css
:root {
  /* Make primary purple */
  --primary: oklch(0.5 0.25 280);
  --primary-foreground: oklch(0.98 0.01 280);

  /* Slower animations */
  --animation-normal: 500ms;
}
```
