---
title: السِّمات
---

<svelte:head>
	<title>السِّمات - FancyUI Docs</title>
</svelte:head>

# السِّمات

تستخدم FancyUI خصائص CSS المخصّصة (فضاء الألوان OKLCh) للتنسيق، وهي متوافقة تماماً مع اصطلاحات shadcn-svelte.

> هل تفضّل ضبطها بصرياً؟ يتيح لك [مُولّد السِّمات](/docs/getting-started/theme-generator) تعديل هذه الرموز باستخدام أشرطة تمرير حيّة ونسخ الـ CSS.

## إعداد CSS

استورد صفحة أنماط FancyUI في ملف CSS الخاص بتطبيقك (مثل `src/app.css`):

```css
@import "tailwindcss";
@import "fancy-ui-svelte/tailwind.css";
```

## نظام الألوان

تستخدم جميع الألوان فضاء الألوان **OKLCh** للحصول على تدرّجات متجانسة إدراكياً:

```css
:root {
  --background: oklch(1 0 0);
  --foreground: oklch(0.129 0.042 264.695);
  --primary: oklch(0.208 0.042 265.755);
  --primary-foreground: oklch(0.984 0.003 247.858);
  --muted: oklch(0.968 0.007 247.896);
  --muted-foreground: oklch(0.554 0.046 257.417);
  --accent: oklch(0.968 0.007 247.896);
  --border: oklch(0.929 0.013 255.508);
  --card: oklch(1 0 0);
  --card-foreground: oklch(0.129 0.042 264.695);
}
```

## الوضع الداكن

يُفعَّل الوضع الداكن عبر الصنف `.dark` على عنصر أب. تتجاوز FancyUI جميع الرموز:

```css
.dark {
  --background: oklch(0.05 0 0);
  --foreground: oklch(0.984 0.003 247.858);
  --primary: oklch(0.929 0.013 255.508);
  --card: oklch(0.1 0.005 265);
  --border: oklch(1 0 0 / 8%);
}
```

## رموز الحركة

تحكّم في توقيت الحركة على مستوى النظام بالكامل:

```css
:root {
  --animation-fast: 150ms;
  --animation-normal: 300ms;
  --animation-slow: 500ms;
  --animation-slower: 1000ms;
}
```

## دوال التخفيف

```css
:root {
  --ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
  --ease-out: cubic-bezier(0, 0, 0.2, 1);
  --ease-in: cubic-bezier(0.4, 0, 1, 1);
  --ease-bounce: cubic-bezier(0.34, 1.56, 0.64, 1);
}
```

## التدرّجات القوسية

تُستخدم بواسطة `RainbowButton` و`GradientButton` وتأثيرات التدرّج الأخرى:

```css
:root {
  --rainbow-1: hsl(0 100% 63%);
  --rainbow-2: hsl(270 100% 63%);
  --rainbow-3: hsl(210 100% 63%);
  --rainbow-4: hsl(195 100% 63%);
  --rainbow-5: hsl(90 100% 63%);
}
```

## التخصيص

تجاوز أي رمز في ملف CSS الخاص بك لتخصيص المظهر:

```css
:root {
  /* Make primary purple */
  --primary: oklch(0.5 0.25 280);
  --primary-foreground: oklch(0.98 0.01 280);

  /* Slower animations */
  --animation-normal: 500ms;
}
```
