---
title: 테마 설정
---

<svelte:head>
	<title>테마 설정 - FancyUI Docs</title>
</svelte:head>

# 테마 설정

FancyUI는 테마 설정을 위해 CSS 사용자 정의 속성(OKLCh 색 공간)을 사용하며, shadcn-svelte 규칙과 완전히 호환됩니다.

## CSS 설정

앱의 CSS(예: `src/app.css`)에서 FancyUI 스타일시트를 가져오세요:

```css
@import "tailwindcss";
@import "fancy-ui-svelte/tailwind.css";
```

## 색상 시스템

모든 색상은 지각적으로 균일한 그라데이션을 위해 **OKLCh** 색 공간을 사용합니다:

```css
:root {
  --background: oklch(1 0 0);
  --foreground: oklch(0.129 0.042 264.695);
  --primary: oklch(0.208 0.042 265.755);
  --primary-foreground: oklch(0.984 0.003 247.858);
  --muted: oklch(0.968 0.007 247.896);
  --muted-foreground: oklch(0.554 0.046 257.417);
  --accent: oklch(0.968 0.007 247.896);
  --border: oklch(0.929 0.013 255.508);
  --card: oklch(1 0 0);
  --card-foreground: oklch(0.129 0.042 264.695);
}
```

## 다크 모드

다크 모드는 상위 요소의 `.dark` 클래스로 활성화됩니다. FancyUI는 모든 토큰을 재정의합니다:

```css
.dark {
  --background: oklch(0.05 0 0);
  --foreground: oklch(0.984 0.003 247.858);
  --primary: oklch(0.929 0.013 255.508);
  --card: oklch(0.1 0.005 265);
  --border: oklch(1 0 0 / 8%);
}
```

## 애니메이션 토큰

애니메이션 타이밍을 전역적으로 제어합니다:

```css
:root {
  --animation-fast: 150ms;
  --animation-normal: 300ms;
  --animation-slow: 500ms;
  --animation-slower: 1000ms;
}
```

## 이징 함수

```css
:root {
  --ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
  --ease-out: cubic-bezier(0, 0, 0.2, 1);
  --ease-in: cubic-bezier(0.4, 0, 1, 1);
  --ease-bounce: cubic-bezier(0.34, 1.56, 0.64, 1);
}
```

## 무지개 그라데이션

`RainbowButton`, `GradientButton` 및 기타 그라데이션 효과에서 사용됩니다:

```css
:root {
  --rainbow-1: hsl(0 100% 63%);
  --rainbow-2: hsl(270 100% 63%);
  --rainbow-3: hsl(210 100% 63%);
  --rainbow-4: hsl(195 100% 63%);
  --rainbow-5: hsl(90 100% 63%);
}
```

## 사용자 정의

원하는 모습으로 만들려면 자신의 CSS에서 원하는 토큰을 재정의하세요:

```css
:root {
  /* Make primary purple */
  --primary: oklch(0.5 0.25 280);
  --primary-foreground: oklch(0.98 0.01 280);

  /* Slower animations */
  --animation-normal: 500ms;
}
```
