# fancy-ui-vue

Vue counterpart of [`fancy-ui-svelte`](https://www.npmjs.com/package/fancy-ui-svelte) —
same components, same visual contract, ported from the Svelte 5 reference
implementation that lives at the root of this repo, built on Vue 3.5 and
Tailwind CSS v4.

Status: 84 of 144 components of `fancy-ui-svelte` are ported so far, alongside
the `sound` family; the cameleon skin engine (FancyProvider + primitives +
skins) will ship on the `fancy-ui-vue/cameleon` subpath. The Svelte package
remains the reference; each Vue component is a faithful transpose (see
`PORTING.md` for the law that governs ports), and every deliberate difference
is listed under [Divergences](#divergences-from-the-svelte-api) below.

## Install

```bash
npm install fancy-ui-vue
```

Peer dependencies: `vue` 3.5.2 or later, `tailwindcss` 4. TypeScript is an
optional peer at 5.0 or later.

## Tailwind setup

```css
/* app.css */
@import "tailwindcss";
@import "fancy-ui-vue/tailwind.css";
@import "fancy-ui-vue/styles.css";
```

`tailwind.css` points Tailwind's scanner at this package's shipped `dist/` so
every utility class the components use is generated in the consumer's build,
and declares the shared token set the components spend. `styles.css` carries
the components' own keyframes and scoped rules.

## Divergences from the Svelte API

Deliberate, package-level differences from the Svelte reference — everything
else is a faithful transpose. Per-component divergences are added here as
each component is ported.

- **`<Name>Props`** hold the component's own props only, never a
  `ButtonHTMLAttributes`-style intersection: native DOM attributes and
  listeners a component does not itself declare flow through `$attrs`
  instead. A `Name`Prop the component reads from the DOM (for example
  `disabled`) is declared as a real prop.
- **`class` is a declared prop** on every component so `cn(…, props.class)`
  applies exactly once and the value never doubles up through `$attrs`.
- **`ref` is exposed via `defineExpose({ ref })`**, on the same node Svelte's
  `ref = $bindable(null)` pointed at, and is absent from `<Name>Props` (`ref`
  is a reserved vnode key, not a prop).
- **Two-way bindables use `v-model:<name>`** with the name kept from the
  Svelte prop (for example `v-model:open`) — there is no default `modelValue`
  alias on the primary bindable.
- **Snippet arguments become scoped-slot object props.** A Svelte
  `Snippet<[A, B]>` becomes a named scoped slot receiving one props object,
  typed with `defineSlots`, rather than positional arguments.
- **Callback props stay props.** `onOpenChange`, `onSelect` and similar are
  called as `props.onX?.()` rather than emitted as Vue events, so devtools
  shows no event pane for them — a deliberate cost for parity with the
  Svelte and React callback shape.
- **Teleport is placed inside the presence-mounted subtree**, the inverse of
  the DOM-portal ordering on the React side: a closed surface emits nothing
  server-side, and Teleport resolves synchronously once the subtree mounts.
- **No `markSurfaceState` equivalent** — surface-state vocabularies
  (`data-state` values) are read directly from the presence binding.
- **Focus-trap composables return a handle** (`returnFocusNow` / `rearm`)
  instead of the imperative methods a Svelte action could expose, since a
  Vue composable has no directive lifecycle to hang them on.
- **Scoped-style keyframes referenced from an inline style or a custom
  property live in a second, unscoped `<style>` block** in the same
  component file, because `<style scoped>` always renames keyframe names.
  As a side effect, a parent's scoped selector can still match a child
  component's root element — permissive only, never used to reach further
  into a child's markup.
- **DOM attributes a component reads become declared Boolean props**
  (for example `disabled`) rather than being read off `$attrs`, so a bare
  `<X disabled>` behaves correctly instead of resolving to the falsy string
  `""`.
- **`v-model` on native inputs defers to IME composition** the way the
  underlying input element already does, rather than updating on every
  keystroke during composition.
- **Composables expose no `destroy()` method.** Cleanup runs through
  `onScopeDispose`, matching Vue's composable lifecycle rather than a
  Svelte action's `{ update, destroy }` return shape.
- **`DropdownMenuContent` keeps an explicit `nextTick()` wait** before
  moving the panel to its edge, matching the Svelte source's `tick()` call
  (the React port dropped the equivalent wait; the Vue port does not).
- **Media-query and similar composables keep an explicit `start`/`stop`
  pair** and accept getter-shaped options, matching the Svelte action's
  shape (the React port's hook-based rewrite of these two points is not
  carried over).
- **Sound preferences hydrate explicitly in `onMounted`** (`hydrateSound()`),
  rather than reading `localStorage` during setup, because Vue's production
  hydration does not patch an attribute mismatch introduced by an
  eagerly-read client-only preference.
- **Generated ids follow Vue's own `useId()` sequence** (`v-0`, `v-1`, …)
  rather than the Svelte or React id sequence — stable per render tree, not
  matched across frameworks.
- **A handful of proven bugs surfaced while porting `fancy-ui-react` were
  fixed upstream on the Svelte source itself**, and the Vue port inherits
  those fixes rather than reproducing the original Svelte behaviour.
- **`inheritAttrs: false` on every component**, with `v-bind="attrs"`
  written only where the Svelte source spreads rest props onto an element —
  parity with both the Svelte and React packages, not the Vue-idiomatic
  default.
- **Vue 3.5 is the floor**, not just the minimum peer range: the port relies
  on reactive props destructuring, `useTemplateRef`, `onWatcherCleanup` and
  `<Teleport defer>`, all introduced in 3.5.
- **`createNow`'s server-rendered label is a sentinel, not a real time.**
  The initial render emits an empty label (matching the React port's `NaN`
  sentinel) so the server-rendered and hydrated output agree by
  construction, rather than committing a wrong clock value that Vue's
  production hydration would otherwise leave in place.
- **dialog**: The `children`, `footer` and `trigger` snippet props are slots: the dialog's body is the default slot, the action row is `#footer`, and the optional trigger is `#trigger`. `{#if trigger}` becomes `v-if="$slots.trigger"`, so the `display: contents` wrapper is still emitted only when a trigger is supplied.
- **dialog**: `open` is a two-way model rather than a bindable prop: write `v-model:open`. There is no `modelValue` alias. `onOpenChange` still fires on every change from any trigger, and a caller who passes `open` and no listener at all still gets a dialog that drives itself.
- **dialog**: NEW, and the one shape where the model is not the bindable: a caller who passes `open` AND an `@update:open` listener that never writes the value back gets a dialog that does NOT close itself. The model defers to the parent whenever a listener is present, so the panel stays mounted and `data-state` stays `open` while the callback has already reported `false`. The bindable prop updates the component's own view in that same shape. Real `v-model:open` (or `open` with no listener) is unaffected. Measured both ways.
- **dialog**: `ref` is not a prop. `ref` is a reserved vnode key in Vue, so the panel element is published on the instance (`defineExpose({ ref })`) and read through a template ref on `<Dialog>`; `DialogProps` carries no `ref` field.
- **dialog**: `class` is typed `HTMLAttributes["class"]` rather than `string`, so the array and object class forms a Vue consumer expects are accepted. It is still merged onto the panel through the same `cn()` call sites, in the same argument order.
- **dialog**: Native attributes and listeners put on `<Dialog>` land nowhere: neither source component spreads `{...restProps}`, so both are closed (`inheritAttrs: false`, no `v-bind="attrs"`). Package-level parity with the source, but it means `<Dialog id="x" data-foo="bar">` drops both silently.
- **dialog**: The panel's `data-state` is an ordinary reactive binding instead of a static literal rewritten imperatively at transition time. Same attribute, same two values (`open` / `closing`), same timing.
- **dialog**: `inert` on a closing panel is set by this package's presence clock as an ATTRIBUTE (`toggleAttribute`) rather than by the framework's own transition machinery. Same effect, and `hasAttribute("inert")` is what to assert against.
- **dialog**: Generated ids come from Vue's own id generator and read `v-0-title` / `v-0-description` instead of the source's shape. They are stable across a server render and its hydration; nothing may depend on the shape, and none of them is ever used as a CSS selector.
- **dialog**: Test-fixture divergence, recorded because the suite is part of the contract: the `falls back to document.body` case calls `trigger.focus()` before opening, which the source case does not. Without it the trap captures `document.body`, step 1 of the return chain succeeds on a still-connected node, and the case tests nothing.
- **dialog**: Internal (not a consumer surface): `DialogSurfaceProps` drops the source's `children` and `ref` fields — the panel's content is the default slot and the panel element is exposed, not a prop.
- **presence**: `PresenceProps` declares the component's own props only — native attributes and listeners (`id`, `role`, `aria-*`, `@click`, …) reach the root `<div>` through `$attrs` instead of through the props type, which the Svelte package folds in via `Omit<HTMLAttributes<HTMLDivElement>, …>`. Placement is unchanged: they are applied where Svelte spreads `{...restProps}`, so `data-state` still wins over a caller-supplied one.
- **presence**: `ref` is not a prop and there is no `bind:ref`. The root element is published on the component instance (`defineExpose({ ref })`); read it with a template ref on the component, e.g. `<Presence ref="panel" />` then `panel.value.ref`. It is `null` while closed and `null` again once a close settles, exactly as the bindable was.
- **presence**: The required `children` snippet becomes the required default slot: `<Presence :open="open"><div>…</div></Presence>`.
- **presence**: `inert={false}` never touches the `inert` attribute at all, instead of letting the framework set it and then overriding it back to `false` as the exit starts. The observable result is the same — no `inert` on a closing panel — but the attribute is never written and then unwritten. `inert` is also written as the ATTRIBUTE (`toggleAttribute`), never the IDL property, so `:not([inert])` selectors and assistive technology see it under every runtime including jsdom.
- **presence**: `onEnterEnd` / `onExitEnd` stay callback props rather than events; a consumer may equivalently write `@enter-end` / `@exit-end`, which the compiler turns back into the same props. They do not appear in the devtools event pane.
- **select**: **`value` is two-way through `v-model:value`** rather than `bind:value`. The Svelte prop name is kept and there is no `modelValue` alias; the three documented call shapes (two-way, `onValueChange` alone, plain value plus callback) all work off the one implementation, and `@value-change` also reaches `onValueChange` because the compiler turns it into that prop.
- **select**: **The trigger element is exposed as `defineExpose({ ref })`**, on the same `<button>` the Svelte `ref = $bindable(null)` pointed at, and `SelectProps` carries no `ref` field (`ref` is a reserved vnode key).
- **select**: **`class` is typed `HTMLAttributes["class"]` instead of `string`**, so an array or object class value is accepted as well as a string; it is still merged onto the trigger through the same single `cn()` call, in the same argument position.
- **select**: **The panel and option ids read `v-N-listbox` / `v-N-option-K`** rather than the Svelte id shape, because the seed comes from the package-level id helper. They are SSR-stable and are only ever `aria-controls` / `aria-activedescendant` targets — nothing may key CSS off them.
- **agent-plan**: The `item` Snippet<[PlanStepData, number]> becomes a named scoped slot `item` carrying object props `{ step, index }` (PORTING.md tuple-to-named-object rule) instead of positional snippet args.
- **agent-plan**: No `ref` prop: Svelte's `ref = $bindable(null)` becomes `defineExpose({ ref })` on the root div, read through a template ref / `wrapper.vm.ref`, not a v-model-style prop. `ref` is absent from AgentPlanProps.
- **agent-plan**: `class` is typed `HTMLAttributes["class"]` rather than Svelte's `string`, so arrays/objects are accepted; `cn()` call site and argument order are unchanged.
- **ai-data-table**: `cell` is no longer a prop: the snippet `Snippet<[unknown, { row: number; key: string }]>` becomes the scoped slot `cell`, tuple parameters named as object props — `<template #cell="{ value, at }">`, with `at` = `{ row, key }`.
- **ai-data-table**: `ref` is not a prop: the Svelte bindable `ref = $bindable(null)` becomes `defineExpose({ ref })` on the scrolling root — read from the component instance, since `ref` is a reserved vnode key in Vue.
- **ai-data-table**: `class` is typed `HTMLAttributes["class"]` rather than `string` (package-wide convention); still merged through `cn()` onto the scrolling root at the same call site.
- **ai-data-table**: `inheritAttrs: false` with no `v-bind="attrs"` anywhere — the source spreads no restProps, so extra native attributes are dropped here as they are ignored there; same observable behaviour, but a Vue consumer's fallthrough attrs never land on the root.
- **animated-beam**: `duration` no longer defaults to a random number. The Svelte default is `Math.random() * 3 + 4`, which a server render and its hydration would disagree on. A new optional `seed` prop (default `1`) feeds a deterministic PRNG that produces the default instead. Two beams with neither `duration` nor `seed` now share one pace — vary `seed` (or pass `duration`) to desynchronise them. Same prop and same default as the React package.
- **animated-beam**: The gradient's `id` comes from `useFancyId()` (Vue's `useId()`), not from `"beam-" + Math.random().toString(36)`. The id is now stable across a server render and its hydration; its rendered shape (`v-0`, `v-1`, …) differs from the Svelte and React packages and nothing may depend on it.
- **animated-beam**: `containerRef`, `fromRef` and `toRef` are typed `HTMLElement | null` rather than a required `HTMLElement`: a Vue template ref is `null` until the parent has mounted, so the idiomatic `:container-ref="container"` binding passes `null` on the first render. They are still required props (pass `null`, not nothing).
- **animated-beam**: `class` is a declared prop typed `HTMLAttributes["class"]` (package-wide convention) instead of `string`.
- **animated-beam**: `AnimatedBeamProps` lives in `AnimatedBeam.vue`'s sibling `<script lang="ts">` block, not in a colocated `types.ts` — the Svelte folder's `types.ts` held nothing but that interface, and PORTING.md C-9 requires it in the SFC. The import path through the package barrel is unchanged.
- **animated-beam**: Native attributes and listeners do not fall through to the `<svg>` (`inheritAttrs: false`, package-wide). The Svelte component spreads no `restProps` either, so no consumer behaviour changes.
- **animated-testimonials**: `class` is a declared prop typed `HTMLAttributes["class"]` rather than Svelte's `string` — package-level, already documented in vue/README.md.
- **animated-testimonials**: Native DOM attributes and listeners are not part of `AnimatedTestimonialsProps`; they flow through `$attrs`. Because the Svelte source spreads no rest props, this component sets `inheritAttrs: false` with no `v-bind="attrs"`, so stray attributes are dropped exactly as in Svelte — package-level note, no component-specific wording needed.
- **animated-testimonials**: The `select` cue reaches the shared controller through `useSoundCue`, which calls `sound.play(cue, options)` — so the spy sees `("select", undefined)` where the Svelte component calls `soundFx.play("select")`. Audibly identical; visible only to a test asserting exact arity. House-wide pattern (agent-plan, approval-card, breadcrumb, navbar, pagination all assert the same).
- **animated-testimonials**: Autoplay watch sources are spelled out (`autoplay`, `isHovered`, `interval`, the `testimonials` identity AND its `length`) to reproduce Svelte's implicit `$effect` dependency set, including a parent that mutates a reactive array in place. No behavioural difference observed.
- **animated-testimonials**: No component-specific line is required in vue/README.md: props, defaults, empty state, a11y attributes and the 300ms transition constant are all preserved, and the Svelte quirks are ported as-is (no `prefers-reduced-motion` handling, `TRANSITION_DURATION` still not a prop, a navigation during a transition is dropped rather than queued).
- **animated-tooltip**: `class` is typed `HTMLAttributes["class"]` rather than `string` — the package-wide rule, no per-component wording needed.
- **animated-tooltip**: Native attributes and listeners placed on `<AnimatedTooltip>` are dropped (`inheritAttrs: false`, no spread). This matches the Svelte component, which spreads no rest props either, so it is parity rather than a new divergence.
- **animated-tooltip**: The tooltip's 200 ms scale/fade runs on the package presence clock instead of `transition:scale`. Same timing, easing (ease-out cubic), start scale (0.6) and composed transform; two consequences: the tooltip node appears one extra pre-paint render pass after the hover (still in the same flush, invisible to the user), and the leaving node carries an `inert` attribute — Svelte sets the reflected `inert` property, so the rendered markup matches.
- **apple-card-carousel**: `AppleCardData.content` is a Vue component or render function (`Component` from `vue`), rendered through `<component :is="card.content">`, where the Svelte package takes a `Snippet`. It cannot become a slot: `content` lives inside an item of the `cards` array, not in a prop position, so there is no 1:1 snippet-to-slot mapping. Consumers pass `content: () => h('div', …)` or a small component instead of `{#snippet}`.
- **apple-card-carousel**: `AppleCard`'s own prop interface is exported as `AppleCardProps` from `AppleCard.vue`; the Svelte source keeps it as an unexported local `interface Props`. Required by the `<Name>Props` tooling contract (C-9). The folder barrel still does NOT re-export it, matching the Svelte `index.ts` and the README line that says the card's prop interface is not re-exported as a named type.
- **apple-card-carousel**: `class` is a declared prop typed `HTMLAttributes["class"]` on both components (package-level rule), so it is pulled out of `$attrs` and merged once through `cn()` at the Svelte call site. Neither component spreads rest props in Svelte, so both stay closed: `inheritAttrs: false` with no `v-bind="attrs"` anywhere.
- **apple-card-carousel**: `onExpand` / `onCollapse` on `AppleCard` remain callback props called directly, not emits (package-level rule); a consumer building a custom shell passes `:on-expand` / `:on-collapse` rather than listening for `@expand`.
- **approval-card**: `bind:state` becomes `v-model:state` (no `modelValue` alias). Vue's `defineModel` only emits while the consumer drives the model, so a private one-tick latch stands in for Svelte's synchronous bindable write: a gate still resolves exactly once, including two decisions dispatched in the same tick under a full `v-model:state`.
- **approval-card**: A fully-controlled consumer that never sends the decision back leaves the card pending with its buttons live, and a decision taken on a later tick is accepted rather than silently refused — Svelte's bindable always writes locally, so it would have shown the verdict regardless of the parent.
- **approval-card**: `ref` is not a prop. The root element is read through the component instance (`defineExpose({ ref })`), not `bind:ref` — package-level D-V2.
- **approval-card**: The `children` snippet becomes the default slot: put the detail region between the tags, `<ApprovalCard …><pre>…</pre></ApprovalCard>`; `children` is absent from `ApprovalCardProps`.
- **approval-card**: `ApprovalCardProps` declares the component's own props only; `class` is a declared prop typed `HTMLAttributes["class"]` (D-V1 / package-level).
- **approval-card**: `inheritAttrs: false` with no attribute spread: an undeclared native attribute or listener lands nowhere, exactly as in the Svelte source, which spreads no rest props (D-V18).
- **approval-card**: `onApprove` / `onDeny` stay `() => void` with no argument — the Vue port keeps the Svelte signature that the React package had to widen to `(state) => void`.
- **approval-card**: HYGIENE (not this folder): `vue/src/components/tool-call/__probe_a.test.ts` is a leftover verifier probe and is currently contributing two errors to the shared `pnpm --filter fancy-ui-vue run check` run.
- **approval-card**: `bind:state` becomes `v-model:state` (no `modelValue` alias). Vue's `defineModel` only emits — it does not write its own copy — while the consumer drives the model, so a private one-tick latch stands in for Svelte's synchronous bindable write: a gate still resolves exactly once, including two decisions dispatched in the same tick under a full `v-model:state`.
- **approval-card**: `onApprove` / `onDeny` stay `() => void` with no argument — the Vue port restores the Svelte signature that the React package had to widen to `(state) => void`.
- **bento-grid**: Snippet props become slots: BentoGrid's `children` is the default slot; BentoGridItem's `header`/`icon`/`title`/`description` and BentoGridCard's `icon`/`background` are named slots (`<template #icon>`), not props. Package-wide convention.
- **bento-grid**: `class` is typed `HTMLAttributes["class"]` (string | array | object) instead of Svelte's `string`; default `""` and the cn() merge point are unchanged.
- **bento-grid**: Native attributes and listeners do not fall through (inheritAttrs:false, no v-bind="attrs") — this MATCHES the Svelte source, which spreads no restProps; listed only so the package-level $attrs note is not read as applying here.
- **bento-grid**: Cosmetic: BentoGridCard's CTA anchor renders `Learn more →` with no surrounding whitespace, where Svelte emits newline+tab around `{cta} &rarr;`. HTML-collapsed, no visual difference; `link.textContent` still contains the cta string.
- **bg-falling-stars**: None found beyond the package-level Props-interface divergence already documented in vue/README.md (no ButtonHTMLAttributes-style intersection; class typed HTMLAttributes['class']). API surface (color, count, class) matches the Svelte wrapper exactly; no seed prop was added since the current Svelte source (over the core, in shared-cores worktree) has no seed prop either — Math.random/Date.now stay confined to the pre-existing core's internal random default param, never called directly by the wrapper.
- **bg-stars**: `seed?: number` prop added (default 1), absent from the Svelte API. The Svelte wrapper scatters stars with `Math.random()` in a client-only effect; the port derives the three layers from a deterministic mulberry32 stream keyed on `seed`. One stream feeds all three `generateStars` calls in layer order, matching the React package, so the same `seed` + `starColor` names the same sky in both packages. Mandated by PORTING.md:68-74; recorded at vue/README.md:155.
- **bg-stars**: The star box-shadows are present in the server-rendered HTML. The Svelte wrapper starts with empty box-shadow strings and paints the sky in an on-mount effect, so the port moves a ~1600-entry box-shadow payload into the SSR output (and removes the first-frame starless flash). Consequence of the seed+PRNG rule. Recorded at vue/README.md:156.
- **bg-stars**: Package-level, already documented: `StarsBackgroundProps` declares the component's own props only, `class` typed `HTMLAttributes["class"]`; the Svelte `children` snippet becomes the default slot, typed with `defineSlots`.
- **bg-stars**: Package-level, already documented: with `inheritAttrs: false` and no `{...restProps}` on the Svelte side, native attributes passed to `<StarsBackground>` are dropped rather than landing on the root element (verified: `id` / `data-*` do not appear). Matches PORTING.md:108 but differs from the plain Vue `$attrs` fallthrough expectation.
- **bg-stars**: Test-only, explicitly permitted by the standing rules: `bg-stars-core.test.ts` is the verbatim Svelte copy with one added non-null assertion (`y!.trimStart()`, line 37) because our tsconfig sets `noUncheckedIndexedAccess` and the Svelte one does not. `bg-stars-core.ts` itself stays byte-identical.
- **bg-stars**: `seed?: number` prop added (default 1), absent from the Svelte API. The Svelte wrapper scatters stars with `Math.random()` in a client-only effect; the port derives the three layers from a deterministic mulberry32 stream keyed on `seed`. One stream feeds all three `generateStars` calls in layer order, matching the React package, so the same `seed` + `starColor` names the same sky in both packages. Mandated by PORTING.md:68-74. Already recorded at vue/README.md:155.
- **bg-stars**: The star box-shadows are present in the server-rendered HTML. The Svelte wrapper starts with empty box-shadow strings and paints the sky in an on-mount effect, so the port moves a ~1600-entry box-shadow payload into the SSR output (and removes the first-frame starless flash). Consequence of the seed+PRNG rule. Already recorded at vue/README.md:156.
- **bg-stars**: Package-level, already documented: `StarsBackgroundProps` declares the component's own props only, `class` typed `HTMLAttributes["class"]`; the Svelte `children` snippet becomes the default slot (now typed with `defineSlots`).
- **bg-stars**: Package-level, already documented: with `inheritAttrs: false` and no `{...restProps}` on the Svelte side, native attributes passed to `<StarsBackground>` are dropped rather than landing on the root element. Matches PORTING.md:108, differs from the plain Vue `$attrs` fallthrough expectation.
- **bg-stars**: Test-only (explicitly permitted by the standing rules): `bg-stars-core.test.ts` is the verbatim Svelte copy with one added non-null assertion, `y!.trimStart()`, because our tsconfig sets `noUncheckedIndexedAccess` and the Svelte one does not. `bg-stars-core.ts` itself stays byte-identical to the shared-cores copy.
- **blur-reveal**: `children?: Snippet` is not a prop: content goes through the default slot (`<slot />`), gated by `v-if="$slots.default"` exactly where Svelte writes `{#if children}`. Slots are typed with defineSlots.
- **blur-reveal**: `class` is typed `HTMLAttributes["class"]` rather than Svelte's `string`, and BlurRevealProps declares the component's own props only - the package-level convention.
- **blur-reveal**: Native attributes and listeners are NOT forwarded (`inheritAttrs: false`, no `v-bind="attrs"`). This is parity, not a loss: the Svelte source destructures no `...restProps`, so it swallows unknown attrs too. Worth one README line because the rest of the package forwards through $attrs.
- **blur-reveal**: Server markup only: with no `class` prop Vue emits `class=""` on the container, where Svelte's SSR `attr_class` (node_modules/svelte/src/internal/server/index.js:210-213) returns '' for an empty result and omits the attribute. Cosmetic, no hydration mismatch, and an artifact of the law-mandated `:class="cn(className)"` call site shared by every port - not specific to this component.
- **blur-reveal**: Server markup only: the style attribute serialises without spaces after the colons (`--blur-reveal-duration:1s;`) where Svelte SSR emits `: 1s`. Purely textual; client-side both go through setProperty.
- **blur-reveal**: Scoped-style selectors use `:deep(*)` where Svelte used `:global(*)`, the compiler-mandated equivalent for slotted descendants; rule bodies are unchanged.
- **book**: No restProps/attrs fallthrough was added since spec marked restSpread false and the Svelte component declares no ...restProps either; consumers needing extra DOM attrs on the outer div have no equivalent (matches Svelte, not a new gap).
- **book**: Svelte source has no standalone types.ts; the color/size/radius/shadow maps and their derived types live directly in index.ts as in the Svelte original, so no separate types.ts file was created.
- **border-beam**: Style attribute changed from a single semicolon-joined string ($derived) to an object :style binding (per PORTING.md rule: string style+custom properties drops props under jsdom); resulting inline style output is equivalent key:value pairs, same custom-property names and values.
- **border-beam**: BorderBeamProps.class typed as HTMLAttributes["class"] instead of Svelte's `string`, per Vue port API contract; functionally equivalent (accepts string, still merged via cn()).
- **box-reveal**: Content is the default slot instead of a Svelte `children: Snippet` prop; it is declared REQUIRED via `defineSlots<{ default(): unknown }>()`, matching the Svelte type `BoxRevealProps & { children: Snippet }` (and the sibling convention in DimSiblings/Presence, whose Svelte children are likewise required). Consumer shape: `<BoxReveal>…</BoxReveal>`.
- **box-reveal**: IntersectionObserver wiring uses the shared `useInView` composable (threshold 0.1, once: true) instead of a hand-rolled onMount observer. Same semantics: fires once at 10% visibility, disconnects on first hit and on unmount.
- **box-reveal**: Attach timing: Svelte calls `observer.observe()` synchronously inside `onMount`; the Vue port attaches one flush later, from `useInView`'s `flush:'post'` watcher on the template ref (mandated by C-1). Observable only to code inspecting the observer in the same tick as mount — hence the added `await nextTick()` in the observe test.
- **box-reveal**: No-IntersectionObserver fallback, inherited from the shared core: `internals/motion/in-view.ts` calls `onChange(true)` when `IntersectionObserver` is undefined, so on a browser lacking the API the Vue port reveals immediately, where the Svelte source would throw.
- **box-reveal**: Native attributes/listeners flow through `$attrs` (package-level divergence); Svelte declares no `...restProps`, so `inheritAttrs:false` with no `v-bind="attrs"` keeps parity — no attribute is forwarded, exactly as in Svelte.
- **breadcrumb**: the `item` snippet is a scoped slot, `#item`, receiving one props object `{ item, index }` instead of the two positional snippet arguments - so `item` is no longer a member of `BreadcrumbProps`. Supplying it still replaces the default rendering for every crumb, the last one included, and `Breadcrumb` still owns truncation and separators.
- **breadcrumb**: the bindable `ref` is exposed with `defineExpose({ ref })` on the `<nav>` rather than being a bindable prop; read it off the component instance. `ref` is absent from `BreadcrumbProps`.
- **breadcrumb**: `BreadcrumbProps` declares the component's own props only, and `class` is one of them (typed `HTMLAttributes["class"]`). The Svelte source spreads no rest props onto the `<nav>`, so - matching it - the component sets `inheritAttrs: false` with no `v-bind="attrs"`: extra attributes and listeners put on `<Breadcrumb>` are not forwarded, exactly as in Svelte.
- **breadcrumb**: the decorative ellipsis `<li>` renders its glyph padded with single spaces where the Svelte and React packages emit it with none - a consequence of Vue's template whitespace condensing a multi-line text node. It is visually identical (the `<li>` is a blockified flex item, so edge whitespace collapses away) and the node stays `aria-hidden`, but a consumer test asserting an exact `textContent` must `.trim()`.
- **breadcrumb**: the `select` cue reaches the sound controller as `play("select", undefined)` rather than `play("select")`, because the shared `useSoundCue` player always forwards its optional options argument. Behaviourally identical; visible only to a spy asserting call arguments.
- **button-group**: Context is exposed as BUTTON_GROUP_CONTEXT (an InternalContext<ButtonGroupContext> object with .provide/.useRequired/.useOptional), not the raw BUTTON_GROUP_CONTEXT_KEY symbol Svelte's setContext/getContext use — this follows the package's existing internals/dom/context.ts convention (same shape used by select, card-3d, confetti in this Vue port) so nested Vue consumers get typed inject() without redeclaring a Symbol per component.
- **button-group**: children (Snippet) becomes an unnamed default <slot/>, per the porting contract; no scoped-slot params needed since ButtonGroup's Svelte children took no arguments.
- **button-group**: ref: HTMLDivElement | null $bindable prop becomes defineExpose({ ref }) per PORTING.md law — not a prop, not in ButtonGroupProps, consistent with every other ported component in this package.
- **button-group**: class prop typed HTMLAttributes["class"] instead of Svelte's plain string, per the package-wide API contract (declared-props-only, no native-attribute intersection).
- **card-3d**: Context key: Svelte used a string key "card3d:mouseEntered" via setContext/getContext; Vue uses a Symbol InjectionKey (CARD3D_CONTEXT.key) created with createInternalContext, per project convention - same getter-function shape (() => boolean), just not string-keyed. Not consumer-visible since CardItem/CardContainer are always used together.
- **card-3d**: CardItem's local `ref` variable (bind:this only, never read or exposed in the Svelte source) was dropped entirely rather than ported as a template ref, since it was a no-op capture and exposesRef is false for this unit.
- **card-3d**: as prop typed as plain `string` (matches Svelte's svelte:element `this={as}` typing) rather than a stricter element-tag union; rendered via Vue's <component :is>.
- **card-3d**: Card3D / CardItem: a `CardItem` rendered outside a `CardContainer` degrades to its rest transform (`translateX(0px) … rotateZ(0deg)`) instead of throwing. The Svelte source calls the `card3d:mouseEntered` context getter unguarded, so the same misuse crashes there with a TypeError. Vue treats a missing provider as "the pointer has never entered", which is exactly the transform the Svelte item renders before hover — so inside a container the behaviour is byte-identical, and only the misuse case differs. Suggested README wording under the Divergences section: "**CardItem outside CardContainer** — Svelte throws (it calls the context getter unguarded); the Vue item renders its rest transform instead, which keeps it server-renderable standalone. Inside a `CardContainer` the two are identical."
- **card-spotlight**: None beyond the package-level ones already documented in vue/README.md (no ButtonHTMLAttributes intersection, native attrs via $attrs). No bindables, no exposed ref, no snippets-with-params, no restSpread beyond inheritAttrs:false — this component has none of those per the unit spec, so no new API surface differs from Svelte's CardSpotlight.
- **chat-error**: `icon` and `children` Snippet props become the named slot `icon` and the default slot (sanctioned snippet->slot rule). Consumer API changes from `icon={renderIcon}` / `<ChatError>text</ChatError>` to `<template #icon>` / default slot content.
- **chat-error**: Minor, inherent to that rule: Vue's <slot> fallback fires when a SUPPLIED slot renders nothing, where Svelte's `{#if children}` does not. Probed: `h(ChatError, null, { default: () => [] })` renders the built-in message/detail fallback; Svelte would render an empty body.
- **chat-error**: `ref` bindable becomes `defineExpose({ ref })` on the root div (useTemplateRef), not a prop — it is absent from ChatErrorProps. Probed: the exposed value is identical to the mounted root element.
- **chat-error**: Native attributes/listeners are NOT forwarded: the Svelte source has no `{...restProps}` (migration-matrix restSpread:false), so no `v-bind="attrs"` was added and `inheritAttrs:false` keeps the component closed. Probed: `data-x`/`id` land nowhere, matching Svelte.
- **chat-error**: `class` is a declared prop typed `HTMLAttributes["class"]` instead of `string`, per the package contract.
- **colourful-text**: Added `seed?: number` prop (default 1). The Svelte source shuffles with Math.random() at component init; Vue SSR requires the server render and its hydration to agree on every colour, so the initial shuffle draws from a seeded mulberry32 stream instead. Mirrors the React sibling's identical divergence (internals-api.md C-7 mandates keeping the seed props and mulberry32 helpers the React port introduced).
- **colourful-text**: Shuffle algorithm changed from the source's `[...colors].sort(() => 0.5 - Math.random())` (an inconsistent comparator, so draw count/order is engine-dependent) to Fisher-Yates over the seeded stream, so a given seed deals the same permutation on every JS engine. Visual behaviour (a shuffled palette reshuffled every 5s) is unchanged; the exact resulting order differs slightly from the source's biased shuffle. Matches the React port's expected permutation exactly.
- **compare**: `StarField` gains a `seed` prop (default `1`) driving a deterministic mulberry32 PRNG in place of the Svelte source's render-path `Math.random()`, per PORTING.md's randomisation rule (same default as the React package). Consumer-visible: two `<StarField />` with the same `starsCount` and no `seed` now render an identical sky, where two Svelte instances each got their own.
- **compare**: `StarField` builds its star list once in setup, so a later `starsCount` or `seed` change does not reshuffle the sky — this matches the Svelte source exactly, and differs from the React port, which re-memoises on both.
- **compare**: Native attributes and listeners do NOT fall through on either `Compare` or `StarField` — the Svelte source declares no `...restProps`, so neither SFC spreads `$attrs`.
- **compare**: `class` is a declared prop typed `HTMLAttributes["class"]` on both components (the package-level typing divergence).
- **compare**: the `firstContent`, `secondContent` and `handle` snippets become same-named slots (`<template #firstContent>` …); passing an empty `#handle` slot suppresses the built-in handlebar, exactly as defining an empty snippet does in Svelte.
- **confetti**: `Confetti`'s `fire()` reads the `options` prop at call time, but Vue hands a REASSIGNED `options` object to the child only on the parent's next render: a handler that does `options = { … }` and calls `fire()` in the same tick bursts with the PREVIOUS object, where the Svelte component bursts with the new one. Mutating the object already passed as `options` behaves identically on both sides, and the reassignment is deferred rather than lost — the next `fire()` after the parent re-renders uses it. Framework-inherent (Vue exposes no synchronous prop flush); pinned by ConfettiFire.test.ts:77-92.
- **confetti**: `Confetti`'s imperative `fire()` is reached through the component's exposed instance — `const c = useTemplateRef('c'); c.value.fire()` — where Svelte reaches the `export function fire` through `bind:this`. PORTING.md:55 reserves `defineExpose` for a `$bindable` ref and is silent on a Svelte `export function`; exposing it is the only way to keep the API reachable, so it follows the file's "where silent, mirror Svelte" clause.
- **confetti**: Neither `Confetti` nor `ConfettiButton` forwards `$attrs`: both set `inheritAttrs: false` and neither template carries `v-bind="attrs"`, because neither Svelte source declares a rest spread. A consumer's `class`, `data-*` or `@click` placed on `<ConfettiButton>` is therefore dropped — exactly as it is in Svelte, so this is parity, not a loss, but it differs from the Vue default a consumer may expect (confirmed: SSR of `<ConfettiButton class="c" data-x="1">` emits a bare `<button>`).
- **confetti**: `Confetti`'s `class` prop is typed `HTMLAttributes["class"]` rather than the README's `string` — the package-wide typing rule (PORTING.md:47), accepting arrays and objects that the Svelte prop does not.
- **confetti**: SSR-SWEEP HAZARD (not an API divergence, but it must reach the registrar): `root:Confetti` needs `{ manualStart: true }` in ssr-sweep.fixtures.ts. With no props the mount auto-fire drives the real canvas-confetti animation loop against a jsdom canvas and throws an uncaught `clearRect` TypeError out of a rAF callback, reddening the hydration sweep for a reason unrelated to the port. `manualStart: true` does not change the server-rendered markup at all, so the two-render comparison still proves what it is meant to prove.
- **container-scroll**: Snippets become named slots: Svelte's `titleContent` / `cardContent` snippet props are Vue named slots `<template #titleContent>` / `<template #cardContent>` (names kept identical).
- **container-scroll**: `class` is typed `HTMLAttributes["class"]` instead of Svelte's `string`, so arrays/objects are accepted in addition to a string; default stays `""` and the `cn()` call site, argument order and literals are unchanged.
- **container-scroll**: The props interface is exported as `ContainerScrollProps` (Svelte keeps its `Props` interface module-local and its index.ts exports only the component). Package-wide Vue convention; no runtime effect.
- **container-scroll**: Package-level (already in the Vue README): the interface declares own props only and `inheritAttrs: false` is set with no `v-bind="attrs"` — matching Svelte, which also has no `...restProps` here, so stray native attributes are dropped in both packages.
- **container-text-flip**: The keyframe is not referenced from an inline style/custom property in this component (only a static animation-name), so it stays in one unscoped <style> block for simplicity; scoping it would also work but Svelte's :global{} wrapper maps most directly to an unscoped block.
- **container-text-flip**: Svelte's {#key currentWord} + {#each} remount is reproduced with v-for :key="`${currentWord}-${index}`" per letter, which forces new DOM nodes (and animation restart) exactly like the keyed Svelte block.
- **container-text-flip**: No stale-closure guard was added for `words.length` inside the interval (the Svelte source has the same closure-over-reactive-prop behavior via runes); the React port's rerender-survives-interval-identity test was NOT ported since it targets a React-specific fix absent from the Svelte source — the Vue port intentionally matches Svelte's simpler behavior, not the React port's.
- **dim-siblings**: `children: Snippet` (required prop) becomes the default slot — `<DimSiblings><a/><a/></DimSiblings>` instead of a snippet prop.
- **dim-siblings**: `ref` is no longer a bindable prop: the root element is reached via `defineExpose({ ref })`, i.e. a template ref on the component (`dim.value.ref`), not `bind:ref`.
- **dim-siblings**: `DimSiblingsProps` declares the component's own props only — it no longer extends `Omit<HTMLAttributes<HTMLElement>, 'class'>`. Native attributes and listeners (`id`, `style`, `data-*`, `aria-*`, …) still land on the root through `$attrs`, but are untyped on the interface. Package-level divergence.
- **dim-siblings**: `class` is typed `HTMLAttributes['class']` (string | array | object) rather than `string`; `cn()` merges it at the same call site.
- **dim-siblings**: Scoped-CSS mapping: the child subject `:global(...)` becomes `:slotted(...)`, and the `:global()` wrappers inside `:has()`/`:not()` arguments are dropped (Vue never scopes pseudo-class arguments, and `:slotted()` there would emit invalid CSS). Net effect: Svelte scopes the root compound, Vue scopes the child compound — only direct children that arrived through the default slot dim. DOM injected into the root from outside the slot would not participate (Svelte's rule would still catch it).
- **dim-siblings**: The folder ships no dedicated renderToString-twice test; the Svelte source has none either and the package-wide sweep (ssr-sweep.fixtures.ts) covers this export. SSR determinism and clean hydration were verified here by throwaway probe, not by a checked-in test.
- **direction-aware-hover**: Content is a default `<slot />` instead of the `children` snippet: `<DirectionAwareHover imageUrl="…"><p>Title</p></DirectionAwareHover>` is unchanged at the call site, but the prop is no longer addressable as `children`.
- **direction-aware-hover**: `DirectionAwareHoverProps` is exported as a type from the component's entry; the Svelte source keeps its `Props` interface module-private.
- **direction-aware-hover**: Extra native attributes and listeners are dropped, not forwarded: the Svelte source spreads no `{...restProps}`, so the Vue SFC sets `inheritAttrs: false` with no `v-bind="attrs"`. Only `class` (a declared prop, merged through `cn()`) reaches the root element. Verified: `data-x` / `id` passed to the component do not appear in the rendered output.
- **direction-aware-hover**: The two `<style>` rules keep their declarations verbatim but lose their document-wide reach. The source wraps both in `:global()`, which — because this package aggregates scoped styles into one `dist/styles.css` every consumer imports once — would force a `.group\/card` 44px min-box and a `*{transition-duration:0.1s!important}` reduced-motion override on every app importing the package, including apps that never render this component. Here they compile to `.group\/card[data-v-…]` and `[data-v-…],[data-v-…-s]`: this component's own elements plus its whole slot subtree. Matches the React package's identical narrowing.
- **direction-aware-hover**: Under `prefers-reduced-motion: reduce` the 0.1s transition override therefore applies to this component and its slot content only, not to the rest of the host page.
- **direction-aware-hover**: While the overlay or the caption is fading out, the node carries the `inert` ATTRIBUTE; the source sets the `inert` IDL property at the same instant. Package-level presence-core convention, consumer-visible only to `:not([inert])` selectors.
- **displacement-text**: `class` is typed `HTMLAttributes["class"]` instead of Svelte's `string` (package-wide convention); default stays `""` and the `cn("relative h-[400px] w-full", className)` call site is unchanged.
- **displacement-text**: Native attributes and listeners are NOT declared props: the Svelte component spreads no `restProps`, so the SFC sets `inheritAttrs: false` and binds no `$attrs` anywhere — same closed-attrs surface as the Svelte component (package-level note).
- **displacement-text**: WebGL-less hosts fail quiet instead of throwing. With no WebGL context available, the Vue wrapper swallows three's `Error creating WebGL context` and leaves the host as the server rendered it — an empty div still carrying `role="img"` and `aria-label={text}`, so the text stays available to assistive tech. The Svelte reference lets the error escape its `$effect`. Same house behaviour the sibling three.js component already has (noise-reveal-core's `createRenderer()` returning null, commented "fail quiet"); the guard sits in the wrapper only because displacement-text-core.ts is byte-gated. Nothing is caught where WebGL exists, so the normal path is identical to the reference.
- **displacement-text**: Suggested README wording: "On a host without a WebGL context the scene is skipped and the labelled host box renders empty, rather than raising." (README not edited — outside the writable folder scope.)
- **editorial-engine**: pullquotes is tracked by ARRAY IDENTITY, like the Svelte $effect: an inline :pullquotes="['...']" hands a new array on every parent update and the engine is torn down and re-measured. The React port deliberately keys on array CONTENT (pullquotes.join) instead, so React is the odd package out and Vue matches Svelte. Consumers who want stability should hoist the array to a constant.
- **editorial-engine**: class is a declared prop typed HTMLAttributes["class"] rather than Svelte's class?: string (package-wide convention). The SFC is inheritAttrs:false with no v-bind="attrs" anywhere, matching the Svelte wrapper, which spreads no restProps either, so no native-attribute forwarding is lost relative to Svelte.
- **editorial-engine**: The EditorialEngineProps doc comment uses the React port's wording ("computed from cached font metrics"), not the Svelte one, which names the upstream measurement library and links its demo. That credit still ships in the byte-gated engine.ts header and in the Svelte README; it is not repeated in the Vue SFC.
- **fireworks-hdr**: `class` accepts `HTMLAttributes["class"]` (string, array or object) where the Svelte prop is `string` — the package-wide typing rule, a widening, not a behaviour change. FireworksHdr.vue:39.
- **fireworks-hdr**: Native attributes and listeners do NOT fall through: `inheritAttrs: false` with no `v-bind="attrs"`, because the Svelte wrapper spreads no rest props. This is the already-documented package-level `$attrs` divergence, nothing specific to this component.
- **fireworks-hdr**: Reduced motion is read through `useReducedMotion()` (internals) instead of the Svelte wrapper's inline `window.matchMedia(...)`: same query string, same guards, same once-at-mount read. Side effect: a `change` listener stays attached for the component's lifetime (removed on unmount) that nothing reads, where Svelte attached none. Not consumer-visible. FireworksHdr.vue:92.
- **fireworks-hdr**: `debug` is `import.meta.env?.DEV === true` rather than Svelte's `import.meta.env.DEV`, so a plain-Node import without the bundler shim yields `false` instead of throwing. Matches the existing house pattern in FluidCursor.vue. FireworksHdr.vue:87.
- **fireworks-hdr**: The IntersectionObserver callback takes `(entries)` and returns early on an empty array, where Svelte destructures `([entry])` and would throw. Required by `noUncheckedIndexedAccess`; unreachable in practice. FireworksHdr.vue:123-127.
- **fireworks-hdr**: OBSERVATION (registrar scope, out of the author's lane): `FireworksHdr` does not yet appear in vue/src/index.ts, vue/src/ssr-sweep.fixtures.ts, vue/migration-matrix.json or vue/README.md.
- **fireworks-hdr**: OBSERVATION (other folders, not mine to delete): leftover probe files exist at vue/src/components/pulse-beam/__vprobe.test.ts, vue/src/components/text-generate-effect/__probe_ssr.test.ts and vue/src/components/terminal-text/__probe2.test.ts — the last one is a live vue-tsc error (TS2307, cannot find '@vue/compiler-sfc').
- **flickering-grid**: The shared-cores Svelte wrapper has no `seed` prop (unlike the React port's invented mulberry32+seed). Math.random is only invoked inside onMounted/resize/setInView, never during setup/render, so C-7 does not force determinism here. Ported as-is with no seed prop, matching the current Svelte wrapper exactly.
- **flickering-grid**: IntersectionObserver callback uses `entries[0]` + explicit undefined-guard instead of `([entry]) => ...` destructure, since vue-tsc's noUncheckedIndexedAccess flags the array-pattern form as possibly-undefined; identical behavior, same fix pattern the React port already uses for the same reason.
- **flip-words**: Native attributes and DOM listeners are not applied. The SFC sets `inheritAttrs: false` and, because the Svelte source spreads no `{...restProps}`, adds no `v-bind="$attrs"` — matching Svelte, where undeclared props are dropped. (Package-level $attrs note.)
- **flip-words**: `class` is a declared prop typed `HTMLAttributes["class"]` instead of Svelte's `string`, so arrays and objects are accepted in addition to a string. It is merged through the same `cn()` call, in the same argument position.
- **flip-words**: The four keyframes (`flipFadeInWord`, `flipFadeInLetter`, `flipEnterWord`, `flipExitWord`) ship from a second, unscoped `<style>` block rather than Svelte's `:global { … }` wrapper. Names and declarations are unchanged and both packages emit them globally; two of them are referenced from inline `style` attributes, which a scoped block would break.
- **flip-words**: Known, deliberately preserved source bug: if `words` becomes empty while an exit animation is in flight, `(index + 1) % 0` stores `NaN` and the component stays blank even after `words` is repopulated. Ported as-is from Svelte (the React package diverges here and guards it); under Vue this additionally logs a dev-only `[Vue warn]: VNode created with invalid key (NaN)`.
- **fluid-cursor**: The props interface is exported as `FluidCursorProps` (the Svelte component keeps its `Props` interface local and exports no props type). It is also re-exported as `FluidCursorAdvancedProps` for the deprecated `FluidCursorAdvanced` alias, matching the React barrel.
- **fluid-cursor**: `class` is typed `HTMLAttributes["class"]` instead of Svelte's `class?: string`, so arrays and objects are accepted as well as a string. Same `cn()` call site, same argument order.
- **fluid-cursor**: Native attributes and listeners are not forwarded onto the root div: the component declares `inheritAttrs: false` and spreads no `$attrs` (the Svelte source has no `{...restProps}` either, so nothing reachable is lost).
- **fluid-cursor**: Live-prop forwarding is narrower by construction: Svelte's `$effect` re-ran on ANY prop change and pushed the (unchanged) five live values, while the Vue `watch` tracks only `fluidColor`, `fluidColors`, `contained`, `interactive`, `allowMultiple` and stays silent when only a mount-only prop changes. The engine sees the same values either way; only a mocked core can observe the difference.
- **focus**: None beyond the package-level Vue convention already documented in vue/README.md (props-only interface, no HTMLAttributes intersection; class/native attrs flow via $attrs).
- **focus**: Word ref collection uses a plain ref<(HTMLSpanElement|null)[]> array written via a template ref-callback (:ref="(el) => setWordRef(el, index)") instead of Svelte's bind:this inside #each — functionally identical, imperative-read-only sink, no consumer-visible API change.
- **frosted-glass**: `children?: Snippet` becomes the default slot: `<FrostedGlass><nav …/></FrostedGlass>` instead of a snippet prop. Vue renders nothing when no slot is passed, so the Svelte `{#if children}` guard is dropped as redundant.
- **frosted-glass**: `class` is a declared prop typed `HTMLAttributes["class"]` (Svelte types it `string`); it still targets the content layer only, and `containerClass` still targets the outer container. Package-level convention, same merge point via cn().
- **frosted-glass**: The SVG filter id minted after mount reads `fg-dist-<counter>` (monotonic `uid()`), not `fg-dist-<random base36 suffix>`; the source's `Math.random()` is banned on this package's render paths. Same choice as the React port. DOM-visible only; the `filter: url(#…)` wiring is unchanged.
- **frosted-glass**: `FrostedGlassProps` declares the component's own props only — no native-attribute intersection — and the SFC sets `inheritAttrs: false`; the Svelte component never spread restProps either, so no native attribute reached the root there and none does here.
- **glare-card**: cssVars is emitted as a Vue :style object ({'--m-x': '50%', ...}) instead of Svelte's single interpolated style string, per PORTING.md rule 'custom properties → object :style, never string interpolation (jsdom drops custom props via cssText)'. Rendered attribute value is behaviorally identical (tests assert substrings of the style attribute and pass). No consumer-visible API change — internal only.
- **glow-border**: Svelte builds the whole inline style as one template-literal string (mask, mask-composite, background-image, etc.). Per PORTING.md's string-style/custom-property trap, this port uses an object :style binding with the same properties/values instead of a cssText string — same rendered style, no consumer-visible difference, purely an implementation-safety divergence for SSR/jsdom parity.
- **glowing-effect**: None in the public API: props, defaults, class names, CSS custom properties and disabled-gate behavior are identical to the Svelte GlowingEffect. `class` is typed HTMLAttributes["class"] per PORTING.md instead of Svelte's `string`, a package-level convention, not a component-specific change.
- **gradient-button**: disabled is now a declared Boolean prop (bound via :disabled) instead of flowing through restProps/$attrs, per PORTING.md's rule that a DOM attribute the component reads must be a declared prop (bare attrs.disabled would be the falsy string ''). onclick is likewise a declared prop (typed (event: MouseEvent) => void, matching the pattern used in Link.vue/InteractiveHoverButton.vue) rather than folded from HTMLButtonAttributes, since GradientButtonProps declares only its own props (no attribute-set intersection) per PORTING.md's API contract section.
- **gradient-button**: CSS custom properties are set via an object :style binding ({'--gb-colors': ..., ...}) instead of a single interpolated style string, per PORTING.md's styling table (a string style goes through cssText and jsdom drops custom properties). The rendered inline style text is equivalent (same property names/values); tests assert via string .toContain() and pass unchanged.
- **gradient-button**: Children render through the default <slot/> instead of a Snippet render call — mechanical Svelte-to-Vue slot mapping, no API shape change for object consumers passing a props object, but Vue consumers write <GradientButton>text</GradientButton> instead of children snippet syntax.
- **hyper-text**: — the Svelte source scrambles with a bare `Math.random()`, which cannot run on a render path. The port draws its scramble characters from a seeded PRNG instead, so two instances with no `seed` scramble alike and the effect is reproducible in a test. Same prop, same default and same generator as the React package.
- **hyper-text**: The Svelte source starts its character array empty and fills it from a post-mount effect, so its server render emits no characters at all; the port seeds the array in `setup`, which keeps the server render and its hydration identical. Visually the difference lasts one frame.
- **hyper-text**: (package-level rule: `inheritAttrs: false`, no `$attrs` spread). This matches the Svelte component, which never spreads `restProps` either — it is listed only so the `class` prop is understood as the single styling entry point.
- **hyper-text**: rather than the source's `string`, per the package-level rule — it accepts the array/object forms Vue allows and is merged by `cn()` at exactly the source's call site.
- **image-trail-cursor**: `class` is typed `HTMLAttributes["class"]` (string | array | object) instead of the Svelte `class?: string`, per the package-wide rule that pulls `class` out of `$attrs` so `cn()` applies it once. Same call site, same base literals, same merge order.
- **image-trail-cursor**: Native attributes and listeners are NOT forwarded to the container. The Svelte component spreads no `restProps`, so it drops them too — parity, but the Vue SFC states it explicitly with `inheritAttrs: false` and no `v-bind="attrs"`.
- **image-trail-cursor**: `touch-action: none` on the container is applied as a bound style object (`:style="{ touchAction: 'none' }"`) rather than a literal style attribute string. Identical computed style in a browser and identical SSR output (`style="touch-action:none;"`); only a DOM-string inspection under jsdom differs.
- **interactive-hover-button**: Svelte's `children` Snippet becomes the Vue default slot; `{#if children}…{:else}{text}{/if}` becomes `<slot>{{ text }}</slot>`, functionally identical (slot content wins, fallback is text) but the prop shape differs per the package-level slots-vs-snippets divergence already noted in README.
- **interactive-hover-button**: `InteractiveHoverButtonProps` declares only own props (text, class, sound, disabled, onclick) per the Vue package convention of not folding in ButtonHTMLAttributes; all other native button attributes (type, aria-label, name, form, etc.) flow through as unenforced `$attrs` rather than typed props — same divergence already recorded for the rest of the Vue package.
- **interactive-hover-button**: `disabled` is declared explicitly (not left to fall through $attrs) because the click handler reads it, matching the documented rule that a DOM attribute the component reads must become a declared prop.
- **letter-pullup**: None functionally: 'class' prop is declared per Vue convention (HTMLAttributes['class']) instead of Svelte's bare string type; consumer-visible surface (words, delay, class) is identical.
- **letter-pullup**: Root <style> is left unscoped (not <style scoped>) because the @keyframes name is referenced only from an inline style attribute, per PORTING.md rule 3 — Vue's scoped compiler would otherwise rename the keyframe and break the inline animation reference. No .letter-pullup-char class styling exists in the Svelte source's <style> block to scope anyway (only the keyframes), so this has no visible effect.
- **line-hover-link**: `aria-label` is spelled `ariaLabel` in `LineHoverLinkProps`. Vue camelises declared prop names, so Svelte's hyphenated key can never be read back off `props` (it silently rendered nothing). Both `ariaLabel="…"` and the template spelling `aria-label="…"` reach the prop and emit `aria-label` on the anchor, matching Svelte's output.
- **line-hover-link**: `children?: Snippet` is the default slot, typed with `defineSlots<{ default?(): unknown }>()`, and is absent from `LineHoverLinkProps` (package-level snippet/slot divergence).
- **line-hover-link**: `class` is typed `HTMLAttributes["class"]` rather than Svelte's `string` (package-level class-prop divergence); `cn()` still applies it exactly once, at the same call site.
- **line-reveal**: `class` is typed `HTMLAttributes["class"]` (ClassValue: string | array | object) instead of Svelte's `class?: string`. Same merge point — `cn('relative w-full', className)` — just a wider accepted type.
- **line-reveal**: No native attribute fallthrough: `inheritAttrs: false` with no `v-bind="attrs"`, because the Svelte source declares no `...restProps` and spreads nothing. Extra attributes/listeners passed to `<LineReveal>` are dropped, exactly as in Svelte.
- **line-reveal**: Line height survives a font-only change. Svelte's `style:font` / `style:line-height` directives re-apply only the declaration whose value changed, so switching to a different weight/family at the same px size re-applies the `font` shorthand (which resets `line-height`) without re-applying the longhand, and the lines drift inside their fixed-height masks. Vue's style patch re-applies every key of the style object on each patch, in declaration order (`font` then `line-height`), so the Svelte bug does not reproduce. Behavioural parity with the React port, reached without its folded `32px/38px` shorthand: the emitted style attribute still reads `font: …; line-height: …`, like Svelte's.
- **line-shadow-text**: Svelte's `let style = $derived(...)` produced a CSS string ('--shadow-color: red'); Vue uses an object :style binding ({'--shadow-color': shadowColor}) per PORTING.md's custom-property rule (string style loses custom props in jsdom/SSR). Same resulting computed style value, no consumer-visible difference in tests.
- **line-shadow-text**: The @keyframes line-shadow block was inside Svelte's `:global {}` wrapper (compiler-only syntax); ported to a second unscoped <style> block in the SFC since it's referenced from the animation shorthand inside the scoped ::after rule, per PORTING.md rule 3.
- **line-shadow-text**: Svelte source has no ...restProps spread and no rest destructure, so no v-bind="attrs" was added to the rendered element — attrs still fall through as native Vue attribute inheritance is disabled via inheritAttrs:false with no explicit fallthrough binding, mirroring that Svelte never forwarded extra attributes here either.
- **link**: `children?: Snippet` becomes the default `<slot />` (typed with `defineSlots`) and is absent from `LinkProps` — the package-level snippet/slot divergence.
- **link**: `ref` is reached through `defineExpose({ ref })` on the `<a>` rather than the Svelte bindable `ref` prop, and is absent from `LinkProps` (`ref` is a reserved vnode key) — the package-level exposed-ref divergence.
- **link**: `class` is a declared prop typed `HTMLAttributes["class"]` instead of Svelte's `class?: string`, so it is merged by `cn()` exactly once — the package-level class divergence.
- **link**: Native attributes and listeners do NOT fall through on this component: the Svelte source declares no `{...restProps}`, so per law there is no `v-bind="attrs"` and `inheritAttrs` is false. Verified by probe: `id`, `aria-label` and a consumer `@click` are all dropped. This is Svelte-faithful, but it is the one component where the package README's general "native attrs flow through $attrs" bullet does not apply — a Vue consumer must pass `:onclick="fn"` (the declared prop), not `@click`. Worth one README bullet.
- **liquid-glass**: `class` is typed `HTMLAttributes["class"]` instead of Svelte's `string` (package rule so `cn()` merges it once and it leaves `$attrs`). `containerClass` keeps Svelte's `string`.
- **liquid-glass**: Native attributes and listeners do not reach the root: `inheritAttrs: false` with no `v-bind="attrs"`, because the Svelte source spreads no `restProps`. Same observable surface as Svelte, but Vue would otherwise auto-inherit them.
- **liquid-glass**: The filter id is minted from the package's incrementing client-only counter (`lg-1`, `lg-2`, …) instead of Svelte's `Math.random().toString(36)`. Still minted in `onMounted`, still unique per instance, never present in server HTML; only the literal id text in the DOM differs.
- **liquid-glass**: The root `style` is applied as an object rather than Svelte's string, so the browser/jsdom reports the same declarations in the same order but `cssText` is never used (a style string drops custom properties under jsdom). `backdrop-filter` still appears only once the filter exists.
- **liquid-glass**: The `children` snippet becomes the default slot (`<LiquidGlass>…</LiquidGlass>` is unchanged for consumers).
- **liquid-glass**: `index.ts` also re-exports the `LiquidGlassProps` type; the Svelte `index.ts` exports only the component.
- **liquid-text**: `class` is typed `HTMLAttributes["class"]` (Vue) rather than `string` (Svelte), per the package-wide rule; any Vue class value is accepted and merged through `cn()` at the same call site.
- **liquid-text**: Native attributes/listeners put on `<LiquidText>` are not forwarded (`inheritAttrs: false`, no `v-bind="attrs"`) — matching the Svelte component, which spreads no rest props either. Nothing added or removed from the public surface.
- **liquid-text**: When `font` is left at its default `""` and no computed family is resolved yet, the fallback span omits `font-family` from its inline style instead of emitting an empty declaration. Vue's SSR stringifier would otherwise write `font-family:;`, which the client-side style patch drops — a server/hydration divergence on the one style value that is empty by default. Rendered-attribute detail only; the resolved family applies identically once measured.
- **logo-cloud**: None found consumer-visible: props, defaults, markup structure and class names match the Svelte components exactly (title/logos/wordmarks/class). The React port added an aria-hidden/empty-alt accessibility fix to AnimatedLogoCloud's cloned marquee tracks that is NOT in the Svelte source; per PORTING.md the Svelte folder is the reference, so this Vue port intentionally mirrors Svelte (no aria-hidden), not the React improvement.
- **marquee**: `MarqueeProps` and `ReviewCardProps` are exported as named types from the folder's `index.ts`; the Svelte package exports neither (its `index.ts` re-exports only the two components, and its README states ReviewCard's "own prop type isn't re-exported as a named type, so consumers writing their own card don't need to match its shape exactly"). The type export is mandated by PORTING's `<Name>Props` tooling contract, so the port is right and the Svelte README wording is the thing that no longer describes the Vue package — worth one README line.
- **marquee**: Native DOM attributes and listeners put on `<Marquee>` or `<ReviewCard>` are dropped rather than landing on the root element: both SFCs set `inheritAttrs: false` and neither adds `v-bind="attrs"`, because neither Svelte file spreads `{...restProps}`. Same observable result as Svelte, but it is the package-level `$attrs` divergence already listed in vue/README.md applying here.
- **marquee**: `ReviewCard` declares no `class` prop (matching the Svelte component, which has none), so unlike every other ported component a `class` passed to it is ignored rather than merged. Intentional fidelity, but it is the one exception to the package README's "`class` is a declared prop on every component" bullet.
- **marquee**: Svelte's `children?: Snippet` becomes the default slot (package-level snippet→slot divergence); `Marquee`'s content is therefore supplied as slot children, not as a `children` prop.
- **matrix-rain**: None found consumer-visible: props (color, speed, density, glyphSize, fadeOpacity, class), defaults, and canvas markup/classes match the Svelte wrapper exactly. No bindables, no exposed ref, no slots in the source, so none added.
- **matrix-rain**: Not ported (by design, following the CURRENT shared-cores Svelte wrapper which has no such gating): the sibling React port added an IntersectionObserver-based pause/resume of the rAF loop when the canvas scrolls out of view. The Svelte wrapper being ported here has no such gating and the engine core exposes no start/stop primitive to hang it off (only setOptions/resize/destroy), so adding it would require editing the frozen core. Left unported per PORTING.md 'port pixel-for-pixel, note Svelte's own gaps rather than improving' — flagged here for the registrar's awareness, not treated as a bug in this port.
- **mosaic-glow**: `MosaicGlowProps` declares the component's own props only. Svelte's type is `BaseProps & Omit<HTMLAttributes<HTMLDivElement>, keyof BaseProps>`; in Vue native div attributes and listeners reach the host through `$attrs` (`inheritAttrs: false` + `v-bind="attrs"` on the host, exactly where Svelte spreads `{...restProps}`), so they are not typed members of the props interface.
- **mosaic-glow**: `children?: Snippet` is gone: content goes in the default slot (`<MosaicGlow>…</MosaicGlow>`). The `.mosaic-glow__content` wrapper is still emitted only when slot content is present (`v-if="$slots.default"`).
- **mosaic-glow**: `ref?: HTMLDivElement | null` is no longer a bindable prop: the host element is exposed with `defineExpose({ ref })`. Consumers read it off the component instance (`mosaicRef.value.ref`), not via `bind:ref`.
- **mosaic-glow**: `class` is a declared prop typed `HTMLAttributes["class"]` (so it accepts arrays/objects as well as a string) and is merged by `cn("mosaic-glow relative overflow-hidden", className)` at the same call site as Svelte.
- **navbar**: FOR vue/README.md: Navbar's `brand`, `children` and `actions` snippets become slots, not props — `#brand`, the default slot, and `#actions`. Each region's wrapper `<div>` is still omitted entirely when its slot is absent (`v-if="$slots.brand"` etc.), so the no-empty-wrapper guarantee is unchanged.
- **navbar**: FOR vue/README.md: NavbarLink's `children` snippet becomes the default slot.
- **navbar**: FOR vue/README.md: NavbarLink's `onclick` stays a declared prop (`:onclick="fn"`), per the package-level callback-props rule. Neither Navbar nor NavbarLink spreads rest attributes (the Svelte source spreads none), so with `inheritAttrs: false` a consumer's `@click` / stray native attributes reach neither element — same surface as the Svelte components.
- **navbar**: FOR vue/README.md: `ref` is exposed via `defineExpose({ ref })` on the `<nav>` (Navbar) and the `<a>` (NavbarLink) — the package-level rule, noted here for the two nodes it lands on.
- **navbar**: Not consumer-visible: the cue goes through `useSoundCue(() => sound)` per internals-api §6.3 rather than calling `soundFx.play("select")` directly, so a `vi.spyOn(sound, "play")` observes `("select", undefined)` — the React port's assertion shape. Guard order is unchanged: disabled → preventDefault, then `if (!current) playCue("select")`, then `onclick?.(event)`.
- **neon-border**: NeonBorderProps drops the Svelte `children?: Snippet` field per house convention (slot content flows through Vue's default <slot/>, not a typed prop) — same pattern as other ported components; consumer-visible API is unchanged (default slot still works).
- **noise-reveal**: `class` is a declared prop typed `HTMLAttributes["class"]` instead of Svelte's `class?: string` (package-wide convention). Default `""` and the `cn('relative h-[400px] w-full', className)` call site are unchanged.
- **noise-reveal**: `inheritAttrs: false` with no `v-bind="attrs"` — the Svelte root spreads no `restProps`, so extra native attributes/listeners are dropped in both packages; parity, but it means `@click` or `id` on `<NoiseReveal>` never reaches the root div.
- **noise-reveal**: The `trigger="view"` IntersectionObserver is attached one microtask after mount instead of in the mount flush: the engine is published into a `shallowRef` from `onMounted`, and the post-flush watcher that owns the observer runs on the following flush. Only observable to code that fires an observer synchronously after render; real viewport entry is unaffected.
- **noise-reveal**: `prefers-reduced-motion` is read through `useReducedMotion()` (started in `onMounted`) rather than a direct `window.matchMedia()` call inside the scene effect. Same value at scene-build time, and like Svelte it is read once at build and not forwarded live through `setOptions`.
- **number-ticker**: No <style> block in the Svelte source, so nothing to port to <style scoped> — component is unstyled beyond the Tailwind literal.
- **number-ticker**: Svelte's untrack(() => animate(target)) inside $effect has no direct Vue API; used watch(source, cb) with flush:'post' whose callback body is untracked by construction (per PORTING.md rune table), functionally equivalent.
- **number-ticker**: Svelte's two $effects (initial-value set + re-animate) both became watch() with flush:'post'; the initial displayValue assignment is additionally set synchronously at setup time (SSR-safe, no DOM access) so server-rendered text matches direction='down' immediately without waiting for a post-flush watcher.
- **pagination**: `page` is a `v-model:page` binding (`defineModel('page', { default: 1 })`), not a plain prop, so it is absent from the exported `PaginationProps` interface and a consumer reading the type no longer sees `page` documented. Passing `:page` together with `@update:page` puts the component in fully controlled mode — the parent must echo the new value back or the nav will not move. Passing `:page` alone keeps it uncontrolled: the prop acts as an initial value that still tracks later prop changes.
- **pagination**: The source's bindable `ref` becomes `defineExpose({ ref })` pointing at the `<nav>`: read it off the component instance (a template ref's `.ref`, or `wrapper.vm.ref` in tests) rather than passing it as a prop. It is therefore absent from `PaginationProps`.
- **pagination**: The `previousLabel` / `nextLabel` snippet props become the named slots `#previousLabel` / `#nextLabel`, with the same `‹ Previous` / `Next ›` fallback content. As Vue slot fallbacks they are wrapped in fragment anchor comments (`<!--[-->…<!--]-->`) in the SSR string; text content and layout are unchanged.
- **pagination**: `PaginationProps` declares the component's own props only — no native attribute set is folded in (package-level convention). With `inheritAttrs: false` and no `v-bind="attrs"` (the source spreads no rest props), stray native attributes are dropped rather than landing on the `<nav>`. This matches the source, which ignores them too.
- **pagination**: The decorative “stands for a run of hidden pages” comment sits immediately before the `v-if` / `v-else` pair instead of inside the ellipsis branch, so in dev builds it is emitted into every `<li>`, page-number ones included (visible in the dev SSR HTML). Comment nodes only — no visual or a11y effect — and production builds strip comments on both sides. Moving it inside the branch would need a `<template v-if>` wrapper, whose fragment anchors are a real DOM/SSR divergence, so the comment placement is the cheaper of the two.
- **pagination**: The `ft-pagination-pop` keyframe is renamed by the scoped-style compiler to `ft-pagination-pop-<hash>`. The single reference is rewritten in lockstep and the documented `--ft-pagination-pop-duration` custom property is untouched, but a consumer cannot redefine the keyframe by its source name.
- **pagination**: `goTo` keeps the source's `if (sound) playCue("select")` double guard — `useSoundCue(() => sound)` already holds the `sound &&` gate, whose canonical shape per internals-api.md:1456-1460 is a bare `playCue("select")`. Behaviourally identical; the only observable difference is that the cue reaches the controller as `play("select", undefined)` rather than `play("select")`.
- **pagination**: `page` is a `v-model:page` binding (`defineModel('page', { default: 1 })`), not a plain prop, so it is absent from the exported `PaginationProps` interface. Passing `:page` together with `@update:page` puts the component in fully controlled mode (the parent must echo the new value back); passing `:page` alone keeps it uncontrolled, with the prop acting as an initial value that still tracks later prop changes.
- **pagination**: `PaginationProps` declares the component's own props only — no native attribute set is folded in (package-level convention). With `inheritAttrs: false` and no `v-bind="attrs"` (the source spreads no rest props), stray native attributes are dropped rather than landing on the `<nav>`, matching the source, which ignores them too.
- **pagination**: The decorative “stands for a run of hidden pages” comment sits immediately before the `v-if` / `v-else` pair instead of inside the ellipsis branch, so in dev builds it is emitted into every `<li>`, including the page-number ones. Comment nodes only — no visual or a11y effect. Moving it inside the branch would require wrapping the branch in a `<template v-if>`, and that fragment adds its own anchor nodes to the DOM and `<!--[-->` markers to the SSR output, which would be a real rendering divergence; the comment placement is the cheaper of the two.
- **pagination**: The `ft-pagination-pop` keyframe is renamed by the scoped-style compiler to `ft-pagination-pop-<hash>`. The single reference to it is rewritten in lockstep and the documented `--ft-pagination-pop-duration` custom property is untouched, but a consumer cannot redefine the keyframe by its source name.
- **pagination**: `goTo` keeps the source's `if (sound) playCue("select")` double guard — `useSoundCue(() => sound)` already holds the `sound &&` gate, whose canonical shape is a bare `playCue("select")`. Behaviourally identical; the only observable difference from the canonical shape is that the cue reaches the controller as `play("select", undefined)` rather than `play("select")`. Kept because it mirrors `Pagination.svelte` line for line.
- **pagination**: `pagination-range.ts` is not listed in `shared-cores.json`, so it is not byte-gated; it differs from the Svelte copy by one comment word only (line 1: “no Svelte” → “no framework”). If the registrar ever adds the file to the hash gate, that line has to be reconciled on the Svelte side first.
- **pixel-loader**: No restProps/native-attrs fold-in: PixelLoaderProps declares only its own props per Vue port convention; native attrs/listeners flow through $attrs (package-level divergence documented elsewhere in README).
- **pixel-loader**: ref: Svelte's bindable `ref` becomes defineExpose({ ref }) via useTemplateRef, not a prop — consumers read it via component instance ref (`.value.ref`) rather than binding.
- **prompt-suggestions**: Snippet arguments become scoped-slot object props: Svelte's `{#snippet item(suggestion, index)}` becomes `<template #item="{ suggestion, index }">`. Slot name and both value names are kept; only the positional-vs-object shape differs (already covered package-wide in vue/README.md).
- **prompt-suggestions**: `ref` is not a prop. Svelte's `bind:this` / `ref = $bindable(null)` becomes `defineExpose({ ref })` on the same root `<div role="group">`; read it from the component instance (`wrapper.vm.ref`), and note it is absent from `PromptSuggestionsProps`.
- **prompt-suggestions**: `PromptSuggestionsProps` declares the component's own props only. Unlike the Svelte version there is no native-attribute intersection, and — because the Svelte source spreads no rest props — the component also declares `inheritAttrs: false` with no `v-bind="$attrs"`, so stray native attributes are dropped exactly as Svelte drops them. `class` is a declared prop so `cn()` applies it once.
- **prompt-suggestions**: `onSelect` stays a callback prop (called as `props.onSelect?.(suggestion, index)`), not a Vue event; `@select` still works because the compiler maps it to the `onSelect` prop, but no event appears in devtools.
- **prompt-suggestions**: Pre-flush timing: `$effect.pre` becomes `watch(() => visible, cb, { flush: 'pre', immediate: true })`. The immediate run happens in `setup` (before the first render) rather than in Svelte's first post-render flush. It is inert by construction — the first run only records the mounted value — so the mount, the SSR output and the false→true replay are unchanged; only a parent that flipped `visible` synchronously between setup and the first flush could tell the difference.
- **pulse-beam**: `PulseBeamProps` declares the component's own props only — it no longer intersects `HTMLAttributes<HTMLDivElement>`. Native attributes and DOM listeners still reach the wrapper through `$attrs` (package-level divergence).
- **rainbow-button**: `ref` is no longer a bindable prop: Svelte's `ref = $bindable(null)` becomes `defineExpose({ ref })`. Read it off the component instance (`rainbowButtonRef.value.ref`), not via a prop. Package-level convention.
- **rainbow-button**: `RainbowButtonProps` declares the component's own props only — no `HTMLButtonAttributes` / `HTMLAnchorAttributes` intersection. Editor completion for native attributes is gone, but runtime behaviour is unchanged: the Svelte component never spread them either, and `inheritAttrs: false` with no `v-bind="attrs"` keeps them off the element exactly as before.
- **rainbow-button**: The `children` snippet becomes the default slot (`<RainbowButton>Get started</RainbowButton>` is unchanged for consumers).
- **rainbow-button**: `@keyframes rainbow` and the `.animate-rainbow` rule ship UNSCOPED in the package stylesheet, because a scoped block renames its keyframes and would break the `before:animate-rainbow` utility. Svelte hash-prefixes the keyframe name; here a consumer stylesheet declaring its own `rainbow` keyframe or `.animate-rainbow` class will collide. (The `.animate-rainbow` class itself was already global in Svelte via `:global()`.)
- **rainbow-button**: The `press` cue goes through the package's `useSoundCue` composable, so `sound.play` is called as `play("press", undefined)` rather than `play("press")`. Behaviour is identical; only a spy's recorded arity differs.
- **rainbow-button**: Inline style is written as an object, so the server-rendered attribute reads `style="--rainbow-speed:2s;"` (no space after the colon) where Svelte emits `style="--rainbow-speed: 2s"`. The parsed value is identical and hydration does not warn.
- **ripple**: None found: Ripple is a pure presentational, static-props component (no bindables, no ref, no slots, no sound, no motion deps beyond CSS animate + motion-reduce). API surface matches the Svelte component exactly (same prop names/defaults).
- **ripple-button**: RippleButtonProps declares only its own props (class, rippleColor, duration, sound, disabled, onclick) with no HTMLButtonAttributes intersection, per the package-level Vue divergence: native attrs/listeners flow through $attrs via v-bind="attrs" (inheritAttrs:false) instead of a folded attribute type.
- **ripple-button**: `disabled` is a declared Boolean prop (bound via :disabled) rather than living inside Svelte's ...restProps, because the component reads it to guard the sound cue; a bare `<RippleButton disabled>` would otherwise reach $attrs as the falsy string "" per the porting convention for DOM attributes the component reads.
- **ripple-button**: `onclick` stays a declared callback prop (matching Svelte's explicit destructure) and is invoked manually in handleClick after ripple creation, rather than relying on Vue's native listener merging via attrs — mirrors GradientButton's existing pattern in this package for the identical Svelte shape.
- **shimmer-button**: Button content is the default slot. Svelte's `children` snippet prop becomes `<slot>`; `children` is absent from `ShimmerButtonProps`.
- **shimmer-button**: Svelte's `onclick` callback prop becomes plain `$attrs` fallthrough: the template binds `@click="handleClick"` before `v-bind="attrs"`, so the press cue runs first and the consumer's listener merges after it in source order — the same order as Svelte's `soundFx.play("press"); onclick?.(event)`. No Vue event is declared.
- **shimmer-button**: `disabled` is a declared Boolean prop rather than read off `$attrs`, because `$attrs.disabled` for a bare `<ShimmerButton disabled>` would be the falsy string `""` and would let the cue play. Covered by the package-level bullet at vue/README.md:78-81.
- **shimmer-button**: `ShimmerButtonProps` holds the component's own props only — no `HTMLButtonAttributes` intersection; native button attributes flow through `$attrs`. Covered by the package-level bullet at vue/README.md:44-48.
- **shimmer-button**: The CSS custom properties are bound as a style OBJECT, as PORTING.md requires for custom properties (a string `style` goes through `cssText`, where jsdom drops custom properties). SSR therefore emits `--spread:90deg;` where Svelte emits `--spread: 90deg;`. Cosmetic; hydration is clean either way.
- **shimmer-button**: A consumer-supplied `style` MERGES with the component's custom properties (Vue `mergeProps`), where Svelte's `{...restProps}` after `style={styleVars}` would overwrite them and break the animation. Inherent to `inheritAttrs:false` + `v-bind="attrs"`, package-wide, and strictly safer than the Svelte behaviour.
- **shimmer-button**: Svelte's `onclick` callback prop becomes plain `$attrs` fallthrough: the template binds `@click="handleClick"` before `v-bind="attrs"`, so the cue runs first and the consumer's listener merges after it in source order. No Vue event is declared.
- **shimmer-button**: `disabled` is a declared Boolean prop rather than read off `$attrs`, so a bare `<ShimmerButton disabled>` correctly suppresses the press cue where the Svelte source's `restProps.disabled === ""` is falsy and lets it play. Covered by the package-level bullet at vue/README.md:78-81.
- **shimmer-button**: The CSS custom properties are bound as a style OBJECT rather than the Svelte source's style string, so SSR emits `--spread:90deg;` where Svelte emits `--spread: 90deg;`. Cosmetic; hydration is clean either way.
- **sidebar**: The `children` snippet props are default slots — `Sidebar`'s body, `SidebarGroup`'s item list, `SidebarItem`'s label and `SidebarFooter`'s name text are all written as default slot content.
- **sidebar**: `SidebarItem`'s `icon` snippet is the `#icon` slot and `SidebarFooter`'s `avatar` snippet is the `#avatar` slot. `{#if icon}` / `{#if avatar}` become `v-if="$slots.icon"` / `v-if="$slots.avatar"`, so the `aria-hidden="true"` decorative wrapper is still emitted only when a glyph or avatar is actually supplied.
- **sidebar**: `SidebarItem` writes the shared `content` snippet out in full in both the `<a>` and `<button>` branches of its `v-if`/`v-else`, because a Vue template has no snippet to hoist. Only one branch ever renders and the emitted markup is identical to the Svelte output.
- **sidebar**: `SIDEBAR_KEY` is an `InjectionKey<SidebarContext>` used with `provide`/`inject` rather than a bare `unique symbol` used with `setContext`/`getContext`. It is still read optionally (`inject(SIDEBAR_KEY, undefined)`), so a `SidebarGroup`, `SidebarItem` or `SidebarFooter` mounted outside a `Sidebar` degrades to `collapsed: false` instead of throwing, exactly as before.
- **sidebar**: `SidebarItem`'s `onclick` stays an ordinary prop (`:onclick="fn"`), not a Vue event. With `inheritAttrs: false` and no rest spread in the source, an `@click` written on the component is dropped — the same outcome an undeclared prop has on the Svelte side. Bind `:onclick` to receive activations.
- **sidebar**: `SidebarGroup`'s heading id comes from `useFancyId()` (Vue's `useId()`, e.g. `v-0`) instead of `$props.id()`. It stays SSR-stable and is still the target of the list's `aria-labelledby`; only the id string's shape differs — covered by the package-level id bullet.
- **smooth-cursor**: The `cursor` snippet prop becomes a named slot: `<SmoothCursor><template #cursor>…</template></SmoothCursor>` instead of `cursor={snippet}`. `SmoothCursorProps` therefore has no `cursor` member.
- **smooth-cursor**: `class` is typed `HTMLAttributes["class"]` (string | array | object) rather than the Svelte `string`; it is still merged through the same `cn()` call site.
- **smooth-cursor**: Native attributes and listeners are not forwarded to the root element (`inheritAttrs: false`, no `v-bind="attrs"`). This matches the Svelte source, which spreads no rest props — nothing is lost relative to it.
- **smooth-cursor**: `prefers-reduced-motion` is read through the package's shared `useReducedMotion()` composable rather than a locally-created `matchMedia`. The mount-time answer is still handed to the engine as a plain creation option (no snap) and only a genuine media-query change is routed through `setOptions({ reducedMotion })`, so both of the source's reduced-motion entry points are preserved.
- **smooth-cursor**: Svelte's `$effect` additionally fires once right after mount with the same `springConfig` the engine was created with; the Vue watcher is non-immediate (LAW: an immediate watch would run in SSR setup) and skips that redundant call. `setOptions({ springConfig })` is a pure assignment in the core, so no observable behaviour changes.
- **smooth-cursor**: Teardown order is mirrored rather than identical: Svelte removes the media-query listener and then destroys the engine, while Vue destroys the engine in `onBeforeUnmount` and the composable's `onScopeDispose` detaches the listener as the scope stops. The core's `destroyed` guard makes the interleaving unobservable.
- **sparkles-text**: Added an optional `seed` prop (default 1), absent from the Svelte API, mirroring the React port's default so both ports agree with no seed given. The Svelte source calls Math.random()/Date.now() inside generateStar() during component init — forbidden on a Vue render path since SSR and hydration would disagree. The initial field and the post-mount regeneration stream each use a seeded mulberry32 PRNG (seed and seed+1).
- **sparkles-text**: Sparkle ids no longer include Date.now(): the source's `${x}-${y}-${Date.now()}-${Math.random()}` mixes a wall-clock read into markup, an SSR hazard on its own. The id is `${x}-${y}-${random()}` (one extra PRNG draw), unique within a field for v-for keying, same as the React port.
- **sparkles-text**: Native attributes and listeners reach the root through $attrs rather than a declared attribute-set intersection (package-level convention, `inheritAttrs: false` with no spread since the Svelte source spreads no restProps).
- **stepper**: `ref` is not a prop. Svelte's `bind:ref` on `Stepper` / `Step` becomes the component instance's exposed `ref` (`defineExpose({ ref })`), read through a template ref on the component — `ref` stays a reserved vnode key and is absent from `StepperProps` / `StepProps`.
- **stepper**: `current` is a v-model: `<Stepper v-model:current="i">` replaces `bind:current`. The Svelte name is kept and there is no `modelValue` alias. `onCurrentChange` still fires alongside the model write, in Svelte's order (clickable guard -> sound changed-only guard -> onStepClick -> write current -> onCurrentChange).
- **stepper**: Native attributes and listeners are NOT forwarded. `StepperProps` / `StepProps` declare the component's own props only, and both SFCs set `inheritAttrs: false` with no `v-bind="attrs"` because the Svelte source spreads nothing — so an `id` or `data-*` put on `<Stepper>` / `<Step>` is dropped rather than landing on the `<ol>` / `<li>`. This matches Svelte's closed attribute surface and the landed ToggleGroup / ToggleGroupItem / ButtonGroup ports. (Corrects the previous report, which claimed these fall through `$attrs`; verified by probe.)
- **stepper**: `Step`'s `children` snippet is the default slot: `<Step label="x"><MyGlyph /></Step>` overrides the bullet's checkmark/number. `Stepper`'s `children` is likewise its default slot.
- **stepper**: `STEPPER_KEY` is not a bare symbol. Svelte exports `STEPPER_KEY: unique symbol`; the Vue barrel exports a context handle from `createInternalContext("StepperContext")` carrying `.key`, `.provide()`, `.useRequired()` and `.useOptional()`. Consumers wiring their own provider/consumer use the handle, not `provide(KEY, …)` directly. Same shape as the package's other compound components.
- **stepper**: Registration settles one `nextTick()` after `render()`, not synchronously. Vue defers a sibling's re-render through its async update scheduler, so index-derived output (step number, `data-status`, `aria-current`, connector colour, the dropped leading connector) repaints on the next tick rather than before `render()` returns as in Svelte. The `<li>` count itself is right immediately. Still pre-paint, so no visual difference — but tests must `await nextTick()`.
- **stepper**: Maintenance divergence (not consumer-visible): Svelte factors the bullet and the label/description into two `{#snippet}`s rendered from four call sites. Vue SFCs have no in-file reusable template fragment, so the Vue template inlines both — the bullet markup appears 3x and the text markup 4x. Rendered output is identical, but any future edit to a bullet or a label must be repeated in up to four places.
- **stepper**: `current` is a v-model: `<Stepper v-model:current="i">` replaces `bind:current`. The Svelte name is kept and there is no `modelValue` alias. `onCurrentChange` still fires alongside the model write, in Svelte's order (sound guard -> onStepClick -> write current -> onCurrentChange).
- **stepper**: Native attributes and listeners are not declared props. `StepperProps` / `StepProps` declare the component's own props only; everything else falls through `$attrs` onto the `<ol>` / `<li>`. (Package-level divergence, same as every other port.)
- **stepper**: `STEPPER_KEY` is not a bare symbol. Svelte exports `STEPPER_KEY: unique symbol`; the Vue barrel exports a context handle from `createInternalContext("StepperContext")` carrying `.key`, `.provide()`, `.useRequired()` and `.useOptional()`. Consumers wiring their own provider/consumer use the handle, not `provide(KEY, …)` directly. This matches the package's other compound components (select, toggle-group, button-group).
- **stepper**: Registration settles one `nextTick()` after `render()`, not synchronously. Vue defers a sibling's re-render through its async update scheduler, so index-derived output (step number, `data-status`, `aria-current`, connector colour, the dropped leading connector) repaints on the next tick rather than before `render()` returns as in Svelte. The `<li>` count itself is right immediately. No visual difference — it still lands pre-paint — but tests must `await nextTick()`.
- **stepper**: Vue's template whitespace condensing removes the single space Svelte's compiler emits between the bullet glyph and its `sr-only` status span, and between the label and description spans. Every affected parent is a flex container (`.ft-step-bullet` is `inline-flex`, `.ft-step-trigger` and the text wrapper are `flex`), so whitespace-only siblings were never rendered and nothing moves; only `textContent` concatenation differs ("3not started" vs "3 not started").
- **stepper**: Maintenance divergence (not consumer-visible): Svelte factors the bullet and the label/description into two `{#snippet}`s rendered from four call sites. Vue SFCs have no in-file reusable template fragment, so the Vue template inlines both — the bullet markup appears 3x and the text markup 4x. Rendered output is identical; a future edit to a bullet or a label must be repeated in up to four places. Factoring them into local sub-components would restore the Svelte shape at the cost of extra files in the folder.
- **terminal-text**: The stream is keyed on the CONTENT of `lines`, never on the array's identity. An inline `:lines="['a', 'b']"` hands the component a freshly allocated array on every parent re-render; keying on identity would clear the pending timeouts, wipe the typed-out text and restart the whole animation each time. The source restarts only when a tracked value really changes, and an inline array literal in a Svelte template is not one. The array is serialised rather than joined, so `['ab']` and `['a', 'b']` — which stream differently — never collide on the same key. An in-place mutation of a reactive `lines` array still restarts the stream, matching the source's tracked index reads. The one remaining gap: a re-allocated array with equal content keeps the current stream running, where the Svelte side would restart it. Same trade-off as the React port.
- **terminal-text**: `TerminalTextProps` holds the component's own props only and the SFC sets `inheritAttrs: false` with no `v-bind="attrs"`, matching the Svelte source's closed prop list — native attributes and listeners are dropped rather than landing on the root div (package-wide rule; same observable outcome as Svelte here, which spreads nothing).
- **terminal-text**: The stream is keyed on the CONTENT of `lines`, never on the array's identity. An inline `:lines="['a', 'b']"` hands the component a freshly allocated array on every parent re-render; keying on identity would clear the pending timeouts, wipe the typed-out text and restart the whole animation each time. The source restarts only when a tracked value really changes, and an inline array literal in a Svelte template is not one. The array is serialised rather than joined, so `['ab']` and `['a', 'b']` — which stream differently — never collide on the same key. The one remaining gap: a re-allocated array with equal content keeps the current stream running, where the Svelte side would restart it. Same trade-off as the React port.
- **text-generate-effect**: Native DOM attributes and listeners are NOT forwarded: with `inheritAttrs: false` and no `v-bind="attrs"`, anything not declared is dropped. This is exact parity with the Svelte source (it spreads no `restProps`) and with the React port, but it narrows the package-level bullet in vue/README.md that says undeclared native attributes flow through `$attrs` — for this component they go nowhere. Worth a README qualifier such as "on components whose Svelte counterpart spreads `{...restProps}`"; no code change.
- **text-generate-effect**: `class` is a declared prop typed `HTMLAttributes["class"]` rather than `string` — the existing package-level bullet already covers it; no component-specific wording needed.
- **text-generate-effect**: Implementation note, not an API change: the per-span `style` is bound as a string, not an object, because Vue re-applies every key of an object style on each patch and would reset words the reveal had already shown. Consumers who read the style attribute see the identical value Svelte writes.
- **text-generate-effect**: No other consumer-visible difference: props, defaults, prop names, the rendered two-div/span structure, the `&nbsp;` suffix and the Svelte README's props table all match 1:1. Nothing to add under "Divergences from the Svelte API" for this component.
- **text-generate-effect**: Housekeeping (outside my folder, reporting only): leftover throwaway probes from earlier verifiers are still on disk and account for several of the tree-wide vue-tsc errors — vue/src/components/direction-aware-hover/__probe-style.test.ts, vue/src/components/fireworks-hdr/__probe_hydrate.test.ts, vue/src/components/link/__probe_ssr.test.ts.
- **text-reveal-card**: TextRevealStars takes a new `seed?: number` prop (default 1). The Svelte source scatters the star field with `Math.random()` on the render path; the port derives the layout from a deterministic mulberry32 PRNG keyed on `seed`, so a server render and its hydration are identical. Same seed = same field; the generator and its call order are bit-identical to the React package's, so a given seed yields the same field in both ports. Matches react/README.md:337.
- **text-reveal-card**: TextRevealCard takes a new `starsSeed?: number` prop, forwarded to the star field, so two cards on one page can be given different layouts. It has no default of its own, so an unset `starsSeed` inherits TextRevealStars' default seed of 1 — two cards with no seed share a layout. Matches react/README.md:336.
- **text-reveal-card**: Snippets become slots: Svelte's `children` snippet is the default slot, `text` and `revealText` are named slots (`<template #text>` / `<template #revealText>`). Package-level convention.
- **text-reveal-card**: Native attributes and listeners do not reach the root of either component (`inheritAttrs: false`, no `v-bind="attrs"`). This matches the Svelte source exactly — neither Svelte component spreads restProps — so it is only the standing package-level divergence, not a component-specific one.
- **text-reveal-card**: No styling divergence: `@keyframes star-drift` is published globally under its source name from a second, unscoped `<style>` block, and `.star-animate` stays scoped (PORTING.md Styling rule 3). Unlike the React port, no anchor class is invented — Vue's scoped compiler gives the guarantee Svelte's does. Nothing to record here.
- **text-reveal-card**: TextRevealStars takes a new `seed?: number` prop (default 1). The Svelte source scatters the star field with `Math.random()` on the render path; the port derives the layout from a deterministic mulberry32 PRNG keyed on `seed`, so a server render and its hydration are identical. Same seed = same field; the generator's call order matches the Svelte one. Matches the React port.
- **text-reveal-card**: TextRevealCard takes a new `starsSeed?: number` prop, forwarded to the star field, so two cards on one page can be given different layouts. Matches the React port.
- **text-reveal-card**: No styling divergence: the `star-drift` keyframe is published globally under its source name, as in the Svelte and React packages (PORTING.md §Styling rule 3 second-unscoped-block pattern). Nothing for the registrar to record beyond the two seed bullets above.
- **timeline**: the `content` snippet is the named scoped slot `#content`, receiving one object prop `{ item }` instead of Svelte's positional `Snippet<[TimelineItem]>` argument — `<template #content="{ item }">…</template>`. (Instance of the package-level snippet-argument bullet; the React port keeps it as a `content` render prop.)
- **timeline**: `class` is a declared prop typed `HTMLAttributes["class"]` (Svelte types it `string`), merged by `cn()` at the same single call site on the outer wrapper. (Package-level bullet.)
- **timeline**: the Svelte source declares no rest props, so the Vue port carries `inheritAttrs: false` with no `v-bind="attrs"` — native attributes put on `<Timeline>` are not forwarded to the root element, exactly as in Svelte. Confirmed: an SSR render with `id`/`data-x` emits neither.
- **toggle**: `children` (Svelte `Snippet`) becomes the default slot: `<Toggle label="Bold"><strong>B</strong></Toggle>`. No scoped parameters, so nothing else changes.
- **toggle**: `pressed` is a Vue model: `v-model:pressed="bold"` replaces `bind:pressed={bold}`. The Svelte name is kept and there is no `modelValue` alias. It is declared by `defineModel`, so it does not appear in the exported `ToggleProps` interface; passing `:pressed="true"` with no listener still works and the component still drives itself (uncontrolled usage unchanged).
- **toggle**: `ref` is not a prop. `bind:ref={el}` becomes a template ref on the component plus the exposed ref: `<Toggle ref="toggleRef" />`, then `toggleRef.value?.ref` is the `HTMLButtonElement`. `ref` is a reserved vnode key in Vue, so `ToggleProps` carries no `ref` field.
- **toggle**: `class` is typed `HTMLAttributes["class"]` instead of Svelte's `string`. No change for a consumer passing a string; arrays and objects are additionally accepted.
- **toggle**: Native attributes and listeners put on `<Toggle>` land nowhere — same as the Svelte component, which spreads no `{...restProps}`. `inheritAttrs: false` with no `v-bind="attrs"` reproduces that exactly, so this is parity with the source rather than the package-level `$attrs` divergence.
- **toggle**: `ToggleHarness.test.svelte` has no Vue counterpart file; its role is an inline `defineComponent` at Toggle.test.ts:15-29 per the PORTING.md test rules. Not exported, not consumer-visible.
- **toggle**: Prop declaration order in `ToggleProps` differs from the Svelte interface (`disabled` first rather than `pressed`), a consequence of `pressed`/`ref`/`children` moving out of the interface. No runtime effect; it only affects docs-table ordering if that table is generated from the interface.
- **toggle**: `pressed` is a Vue model: `v-model:pressed="bold"` replaces `bind:pressed={bold}`. The name is kept and there is no `modelValue` alias. It is declared by `defineModel`, so it is absent from the exported `ToggleProps` interface; passing `:pressed="true"` without `v-model` still works and the component still owns its own state (uncontrolled usage is unchanged).
- **toggle**: `ref` is not a prop. `bind:ref={el}` becomes a template ref on the component plus the exposed `ref`: `<Toggle ref="toggleRef" />`, then `toggleRef.value?.ref` is the `HTMLButtonElement`. `ref` is a reserved vnode key in Vue, so it cannot appear in `ToggleProps`.
- **toggle**: `class` is typed `HTMLAttributes["class"]` instead of `string`. No change for a consumer passing a string; arrays and objects are additionally accepted.
- **toggle**: Native button attributes still do not fall through — same as the Svelte component, which has no `{...restProps}`. `inheritAttrs: false` with no `v-bind="attrs"` reproduces that exactly, so this is parity rather than the package-level `$attrs` divergence.
- **toggle**: `ToggleHarness.test.svelte` has no Vue counterpart file; its role is filled by an inline `defineComponent` in `Toggle.test.ts` per the PORTING.md test rules. Not exported, not consumer-visible.
- **toggle-group**: Native attributes and listeners are not forwarded. Neither Svelte component spreads `{...restProps}`, so both SFCs carry `inheritAttrs: false` with no `v-bind="attrs"`: anything beyond the declared props (an `id`, a `data-*`, an extra listener) is dropped rather than landing on the root. Same behaviour as the Svelte source; covered by the package-level `$attrs` note.
- **toggle-group**: `value` is a model, not a prop: Svelte's `bind:value` becomes `v-model:value`, declared with `defineModel('value', { default: '' })` and therefore absent from the exported `ToggleGroupProps` interface. `onValueChange` is still called, after the model write, in Svelte's order.
- **toggle-group**: `ref` is exposed on the instance instead of being a bindable prop: `defineExpose({ ref })` on the same `<div>` / `<button>` Svelte bound, read as `wrapper.vm.ref`, and absent from `ToggleGroupProps` / `ToggleGroupItemProps` (`ref` is a reserved vnode key).
- **toggle-group**: The `children` snippet becomes the default slot on both components; `ToggleGroupItem`'s `label ?? value` text fallback is the slot's fallback content, so it shows only when no slot content is supplied.
- **toggle-group**: The roving tab stop lands one microtask after mount rather than inside the mounting turn. Each item joins the roving order in `onMounted` — the only Vue phase carrying both halves of Svelte's `$effect` (never runs on the server, first client run lands after the DOM exists). The registry is therefore still empty while the tree first renders, so server and client agree on `tabindex="-1"` for every item and the correct `0` is patched in on the next microtask: before paint, with no hydration mismatch. Only a synchronous assertion straight after `render()` can observe the gap, which is why four mount-time `tabindex` tests await one tick; nothing driven by an event needs it.
- **toggle-group**: `TOGGLE_GROUP_KEY` changes shape. Svelte exports a bare `unique symbol` read with `getContext(TOGGLE_GROUP_KEY)`; the Vue package exports an `InternalContext<ToggleGroupContext>` object, so a hand-written item reads the group with `TOGGLE_GROUP_KEY.useOptional()` (and the raw `InjectionKey` stays available as `TOGGLE_GROUP_KEY.key`). The `ToggleGroupContext` interface itself is unchanged.
- **toggle-group**: The roving tab stop lands one microtask after mount rather than in the mounting turn. Each item joins the roving order in `onMounted` — the only Vue phase carrying both halves of Svelte's `$effect` (never runs on the server, first client run lands after the DOM exists). The registry is therefore still empty while the tree first renders, so server and client agree on `tabindex="-1"` for every item and the correct `0` is patched in on the next microtask: before paint, with no hydration mismatch, exactly as Svelte's post-hydration effect flush behaves. Only a synchronous assertion straight after `render()` can observe the gap, which is why four mount-time `tabindex` tests await one tick (`settleRovingOrder()`); nothing driven by an event needs it.
- **tool-call**: `input`, `output` and `icon` are slots rather than snippet props. `input` and `output` are scoped slots: the snippet's single tuple parameter arrives as a named object prop — `<template #input="{ value }">` / `<template #output="{ value }">`.
- **tool-call**: `open` is two-way through `v-model:open` instead of `bind:open`; the Svelte name is kept and there is no `modelValue` alias. `onToggle` stays a callback prop and is also reachable as `@toggle`.
- **tool-call**: `ref` is not a prop: the root `HTMLDivElement` is exposed on the component instance as `ref` (template ref on `<ToolCall>`, then `.ref`) instead of Svelte's bindable `ref` prop.
- **tool-call**: Native attributes and listeners are NOT forwarded to the root element (`inheritAttrs: false`, no `v-bind="$attrs"`). This matches the Svelte source, which declares no rest spread and forwards nothing either — parity, not a new restriction.
- **tool-call**: Slot presence (`input`/`output` provided or not) is read non-reactively off `useSlots()`, so a slot added or removed by a parent `v-if` after mount will not re-evaluate the `hasRequest`/`hasOutput` gates. In Svelte those are reactive prop reads. Edge case only.
- **tool-call**: Generated ids read `v-0-header`, `v-0-body`, … (`useFancyId()` wrapping Vue's `useId()`), not the Svelte `$props.id()` shape. Package-level convention; nothing may depend on the shape.
- **tool-call**: The auto-open for a failure is keyed on the `call.status` VALUE (`onMounted` + `watch(() => call.status, …, { flush: 'post' })`), not on the `call` object identity. Handing the card a fresh `call` object whose status is still `"error"` does not re-fire the auto-open, where Svelte's prop-identity tracking would re-run its `$effect`. Same behaviour as the React port. Observable only when a consumer drives `open` from outside AND re-creates the `call` object.
- **tool-call**: MINOR (cosmetic, no visual effect): Vue's whitespace condensing leaves a space either side of two multi-line text nodes — the "Request" section label and the "Nothing recorded yet." placeholder render as `> Request <` / `> Nothing recorded yet. <` where Svelte emits them tight. HTML collapses the whitespace; textContent differs by the surrounding spaces.
- **tool-call**: `input`, `output` and `icon` are slots rather than snippet props. `input` and `output` are scoped slots: the snippet's single tuple parameter arrives as a named object prop, `<template #input="{ value }">` / `<template #output="{ value }">`.
- **tool-call**: The source's mount-time `$effect` is reproduced as `onMounted(openOnFailure)` alongside the `flush: 'post'` watcher on `call.status` (a watcher cannot carry `immediate: true` here: it would run in SSR setup and write `open` / fire `onToggle` on the server). Behaviour is identical to Svelte; the auto-open for an already-failed call therefore lands after hydration when `open` is bound from outside, exactly as in the source.
- **tracing-beam**: Multiple TracingBeam instances on one page share the literal gradient id 'tracing-beam-gradient', so the second instance's beam is driven by the first instance's spring values — this reproduces the Svelte source's own bug rather than the React port's per-instance useFancyId fix, per the porting law to preserve Svelte behavior and note it rather than silently diverge.
- **typing-indicator**: Native attrs/listeners flow through $attrs (package-level divergence, not component-specific).
- **typing-indicator**: No bindables other than `ref`; component is otherwise API-identical to the Svelte version.
- **editorial-engine**: No consumer-visible divergence from the Svelte wrapper in a browser: `document.fonts` always exists there, so `fontsReady` is the same promise the Svelte effect awaits and boot timing is identical. The only difference appears in an environment with no font loading API, where the Svelte wrapper throws out of its effect and the Vue wrapper instead settles immediately and lets the engine try — widths then come from whatever font is already resolved. Recorded as a code comment on the guard; the vue/ component folders carry no README files (0 of them), so there is no README to word it in.
