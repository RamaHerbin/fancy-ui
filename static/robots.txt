# allow crawling everything by default
User-agent: *
Disallow:

# Machine-readable docs for LLMs and agents:
#   https://fancy-ui.rama.app/llms.txt        index, links to one Markdown page per component
#   https://fancy-ui.rama.app/llms-full.txt   every component: import, props, a working example
#   https://fancy-ui.rama.app/registry.json   the same facts as JSON

Sitemap: https://fancy-ui.rama.app/sitemap.xml
