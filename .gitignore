node_modules

# Vendor (Vue reference sources, local only)
vendor/

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/dist

# Tools
.claude/
.playwright-mcp/
CLAUDE.md

# App-specific routes and components (not part of the component library)
src/routes/portfolio/
src/routes/auth/
src/routes/pages/
src/lib/portfolio/

# Playwright
test-results/
playwright-report/

# Content (builder data, local only)
content/pages/*.json
content/site.json
static/portfolio/

# Synthwave raw assets (multi-MB AI-generated source PNGs; run `pnpm build:assets` to regenerate the shipped avif/webp)
# Kept OUTSIDE static/ so the build never copies them into the published output.
raw-assets/

# Internal docs
/docs/
TODO.md

# Editor
.vscode/

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

*storybook.log
storybook-static
