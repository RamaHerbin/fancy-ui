---
"fancy-ui-react": patch
---

The shared `mosaic-glow` core module now also carries the engine section extracted on the Svelte side (kept byte-identical); the React wrapper does not use it yet. No API change.
