---
"fancy-ui-svelte": patch
---

Docs site: the Theme menu now offers System, Light and Dark only. The eleven named colour themes are gone — cameleon skins own art direction now. The docs-only theme registry no longer ships in the package files (it was never exported).
