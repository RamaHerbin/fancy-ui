---
"fancy-ui-svelte": patch
---

README: an animated preview of the components replaces the static hero image, and the README is brought up to date — 146 components, the React edition (`fancy-ui-react`) with its setup, the docs built for coding agents (per-component Markdown pages and `registry.json`), and a Quick Start example that renders on its own.
