---
"fancy-ui-svelte": patch
---

Landing page: the LiquidGlass panel now demonstrates the effect instead of posing for it. The test backdrop — grid, ivory disc, barcode stripes — scrolls three stage-heights inside the panel while the glass pill stays centred, so the chromatic displacement actually moves. The panel at rest is unchanged.
